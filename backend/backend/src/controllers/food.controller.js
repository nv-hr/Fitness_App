import * as foodRepo from '../repositories/food.repository.js';import { successResponse, errorResponse } from '../utils/response.js';import { ValidationError } from '../utils/errors.js';import { calculateCalories, validateFoodData, validateCustomFoodData } from '../services/food.service.js';import { findByUserId as findProfileByUserId } from '../repositories/profile.repository.js';import { calculateTdee, getCalorieTarget } from '../services/profile.service.js';import { markItemLogged } from '../repositories/dailyMealPlan.repository.js';const VALID_MEAL_TYPES = ['breakfast', 'lunch', 'dinner', 'snack'];/** * GET /api/food/search?q= G현 Search foods by name (FOOD-01, FOOD-02). */export async function searchFoods(req, res, next) {  try {    const q = req.query.q;    if (!q || q.length < 2) {      return errorResponse(res, 'Search query must be at least 2 characters', 400, 'VALIDATION_ERROR');    }    const foods = await foodRepo.searchFoods(req.user.userId, q);    return successResponse(res, foods);  } catch (err) {    if (err instanceof ValidationError) {      return errorResponse(res, err.message, 400, 'VALIDATION_ERROR');    }    next(err);  }}/** * POST /api/food G현 Create custom food for authenticated user (FOOD-03). */export async function createCustomFood(req, res, next) {  try {    const { name, calories_per_100g, category } = req.body;    const validated = validateCustomFoodData({ name, calories_per_100g, category });    const food = await foodRepo.createCustomFood(req.user.userId, validated);    return successResponse(res, food, 201);  } catch (err) {    if (err instanceof ValidationError) {      return errorResponse(res, err.message, 400, 'VALIDATION_ERROR');    }    next(err);  }}/** * POST /api/food/log G현 Log a food entry (LOG-01). */export async function logFood(req, res, next) {  try {    const { foodId, customFoodName, portionGrams, logDate, mealType, calories: clientCalories } = req.body;    // Validate portion    if (!portionGrams || portionGrams < 1 || portionGrams > 5000) {      return errorResponse(res, 'Portion must be between 1-5000 grams', 400, 'VALIDATION_ERROR');    }    // Validate meal type    if (!mealType || !VALID_MEAL_TYPES.includes(mealType)) {      return errorResponse(res, 'Invalid meal type', 400, 'VALIDATION_ERROR');    }    // Default logDate to today    const logDateValue = logDate || new Date().toISOString().split('T')[0];    // Only allow logging for today    const todayStr = new Date().toISOString().split('T')[0];    if (logDateValue !== todayStr) {      return errorResponse(res, 'Can only log food for today', 400, 'VALIDATION_ERROR');    }    let calories;    if (foodId) {      // Seeded food: calculate server-side from calories_per_100g (T-04-06)      const food = await foodRepo.getFoodById(foodId);      if (!food) {        return errorResponse(res, 'Food not found', 404, 'NOT_FOUND');      }      calories = calculateCalories(food.calories_per_100g, portionGrams);    } else if (customFoodName) {      // Custom one-off: use client-supplied calories (CR-03 fix: allow zero-calorie foods)      if (clientCalories == null || clientCalories < 0) {        return errorResponse(res, 'Calories are required', 400, 'VALIDATION_ERROR');      }      calories = clientCalories;    } else {      return errorResponse(res, 'foodId or customFoodName is required', 400, 'VALIDATION_ERROR');    }    const log = await foodRepo.createFoodLog(req.user.userId, {      foodId,      customFoodName,      calories,      portionGrams,      logDate: logDateValue,      mealType,    });    try {      if (foodId) {        await markItemLogged(req.user.userId, logDateValue, mealType, foodId, true);      }    } catch (err) {      console.error('Failed to sync food log to meal plan:', err.message);    }    return successResponse(res, log, 201);  } catch (err) {    if (err instanceof ValidationError) {      return errorResponse(res, err.message, 400, 'VALIDATION_ERROR');    }    next(err);  }}/** * GET /api/food/summary?date= G현 Daily calorie summary with balance (LOG-02, LOG-03, LOG-06). */export async function getDailySummary(req, res, next) {  try {    const date = req.query.date || new Date().toISOString().split('T')[0];    // Get total consumed (pg returns DECIMAL as string G현 ensure number)    const rawTotal = await foodRepo.getDailyTotal(req.user.userId, date);    const totalConsumed = Number(rawTotal);    // Get user profile for calorie target calculation    const profile = await findProfileByUserId(req.user.userId);    let calorieTarget = null;    if (profile) {      const tdee = calculateTdee(profile.weight_kg, profile.height_cm, profile.age, profile.gender, profile.activity_level);      if (tdee) {        calorieTarget = getCalorieTarget(tdee, profile.fitness_goal, profile.calorie_rate);      }    }    const remaining = calorieTarget !== null ? calorieTarget - totalConsumed : null;    const isExtremeDeficit = totalConsumed < 1200;    return successResponse(res, {      date,      totalConsumed,      calorieTarget,      remaining,      isExtremeDeficit,    });  } catch (err) {    next(err);  }}/** * GET /api/food/logs?date= G현 Individual log entries for a date. */export async function getDailyLogs(req, res, next) {  try {    const date = req.query.date || new Date().toISOString().split('T')[0];    const logs = await foodRepo.getDailyLogs(req.user.userId, date);    return successResponse(res, logs);  } catch (err) {    next(err);  }}/** * GET /api/food/history?days= G현 Calorie history for past days (LOG-04). */export async function getLogHistory(req, res, next) {  try {    const days = parseInt(req.query.days, 10) || 7;    const history = await foodRepo.getLogHistory(req.user.userId, days);    return successResponse(res, history);  } catch (err) {    next(err);  }}/** * GET /api/food/recent G현 Recently logged foods for quick-add (LOG-05). */export async function getRecentFoods(req, res, next) {  try {    const recentFoods = await foodRepo.getRecentFoods(req.user.userId);    return successResponse(res, recentFoods);  } catch (err) {    next(err);  }}export default {  searchFoods,  createCustomFood,  logFood,  getDailySummary,  getDailyLogs,  getLogHistory,  getRecentFoods,};