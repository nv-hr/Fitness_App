import { levenshteinDistance } from './string.js';export function fuzzyMatchFoodName(name, dbFoods) {  if (!name || typeof name !== 'string') {    return { matched: false, food: null, matchType: 'none' };  }  const normalized = name.trim().toLowerCase();  if (normalized.length === 0) {    return { matched: false, food: null, matchType: 'none' };  }  const exact = dbFoods.find(f => f.name.toLowerCase() === normalized);  if (exact) {    return { matched: true, food: exact, matchType: 'exact' };  }  const contains = dbFoods.find(f =>    f.name.toLowerCase().includes(normalized) || normalized.includes(f.name.toLowerCase())  );  if (contains) {    return { matched: true, food: contains, matchType: 'case-insensitive' };  }  let bestMatch = null;  let bestDistance = 4;  for (const food of dbFoods) {    const dist = levenshteinDistance(normalized, food.name.toLowerCase());    if (dist < bestDistance) {      bestDistance = dist;      bestMatch = food;    }  }  if (bestMatch) {    return { matched: true, food: bestMatch, matchType: 'levenshtein' };  }  return { matched: false, food: null, matchType: 'none' };}export function recalculateDayCalories(day) {  if (!day || !Array.isArray(day.meals)) return;  let dayTotal = 0;  for (const meal of day.meals) {    if (!Array.isArray(meal.items)) continue;    let mealTotal = 0;    for (const item of meal.items) {      mealTotal += item.calories || 0;    }    meal.total_calories = mealTotal;    dayTotal += mealTotal;  }  day.total_calories = dayTotal;}