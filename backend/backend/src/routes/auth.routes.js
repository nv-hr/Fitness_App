import { Router } from 'express';import authController from '../controllers/auth.controller.js';import { authenticateToken } from '../middlewares/auth.middleware.js';const router = Router();// POST /api/auth/register G현 Register new userrouter.post('/register', authController.register);// POST /api/auth/login G현 Login with email/passwordrouter.post('/login', authController.login);// POST /api/auth/logout G현 Logout (clear session cookie)router.post('/logout', authController.logout);// GET /api/auth/me G현 Get current user (protected)router.get('/me', authenticateToken, authController.getMe);export default router;