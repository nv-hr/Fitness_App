import { Router } from 'express';import foodController from '../controllers/food.controller.js';import { authenticateToken } from '../middlewares/auth.middleware.js';const router = Router();// All food routes require authenticationrouter.use(authenticateToken);// GET /api/food/search?q= G현 Search foods by namerouter.get('/search', foodController.searchFoods);// POST /api/food G현 Create custom foodrouter.post('/', foodController.createCustomFood);// POST /api/food/log G현 Log a food entryrouter.post('/log', foodController.logFood);// GET /api/food/summary?date= G현 Daily calorie summaryrouter.get('/summary', foodController.getDailySummary);// GET /api/food/logs?date= G현 Individual log entries for a daterouter.get('/logs', foodController.getDailyLogs);// GET /api/food/history?days= G현 Calorie historyrouter.get('/history', foodController.getLogHistory);// GET /api/food/recent G현 Recently logged foodsrouter.get('/recent', foodController.getRecentFoods);export default router;