import { Router } from 'express';import profileController from '../controllers/profile.controller.js';import { authenticateToken } from '../middlewares/auth.middleware.js';const router = Router();// All profile routes require authenticationrouter.use(authenticateToken);// POST /api/profile GÇö Create profile (first-time setup)router.post('/', profileController.createProfile);// GET /api/profile GÇö Get existing profilerouter.get('/', profileController.getProfile);// PUT /api/profile GÇö Update profilerouter.put('/', profileController.updateProfile);export default router;