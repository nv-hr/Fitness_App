import { Router } from 'express';import weeklyPlanController from '../controllers/weeklyPlan.controller.js';import { authenticateToken } from '../middlewares/auth.middleware.js';import weeklyPlanLimiter, { regenerateLimiter, swapLimiter, toggleCompleteLimiter } from '../middlewares/weeklyPlanRateLimiter.js';const router = Router();router.use(authenticateToken);router.get('/', weeklyPlanController.get);router.post('/generate', weeklyPlanLimiter, weeklyPlanController.generate);router.post('/regenerate-day', regenerateLimiter, weeklyPlanController.regenerateDay);router.post('/swap', swapLimiter, weeklyPlanController.swap);router.post('/toggle-complete', toggleCompleteLimiter, weeklyPlanController.toggleComplete);export default router;