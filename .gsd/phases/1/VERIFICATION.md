## Phase 1 Verification

### Must-Haves
- [x] Target goal setting form on onboarding — VERIFIED (The conditional `isUpdate &&` wrapping the Goal Weight Subcard Panel in `ProfileForm.jsx` was removed so that the form displays to new users during onboarding).

### Verdict: PASS
