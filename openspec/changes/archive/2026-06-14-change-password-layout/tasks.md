## 1. Routing Adjustments

- [x] 1.1 Update `App.jsx` or the relevant routing configuration to move the `/change-password` route from the dashboard layout wrapper to the authentication layout wrapper.

## 2. Component Styling and Flow

- [x] 2.1 Remove any dashboard-specific container styles from the `ChangePassword` component.
- [x] 2.2 Ensure the `ChangePassword` component uses the standard authentication card layouts and styles to match the login/register pages.
- [x] 2.3 Verify and update redirection flows so the user returns to the dashboard or profile upon successful password change.
