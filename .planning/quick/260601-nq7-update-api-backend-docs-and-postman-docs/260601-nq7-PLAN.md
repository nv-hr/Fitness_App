---
phase: quick
plan: 260601-nq7
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/docs/API.md
  - docs/Fitness_App_API.postman_collection.json
autonomous: true
requirements: []

must_haves:
  truths:
    - API.md documents all route groups that exist in the codebase (including Progress/Weight)
    - Postman collection has requests for all backend API endpoints
    - Rate limit table in API.md is accurate
  artifacts:
    - path: "backend/docs/API.md"
      provides: "Complete backend API reference"
      min_lines: 1300
      contains: "### 10. Progress"
    - path: "docs/Fitness_App_API.postman_collection.json"
      provides: "Postman testing collection with all endpoints"
      min_lines: 1600
      contains: "Progress/Weight"
  key_links:
    - from: "backend/docs/API.md"
      to: "backend/src/routes/progress.routes.js"
      pattern: "Progress"
    - from: "docs/Fitness_App_API.postman_collection.json"
      to: "backend/src/routes/progress.routes.js"
      pattern: "progress"
---

<objective>
Update `backend/docs/API.md` and `docs/Fitness_App_API.postman_collection.json` to document all existing API endpoints.

**What:** Add the missing Progress/Weight tracking section to API.md, and add all 21 missing endpoint requests to the Postman collection.

**Purpose:** Docs and Postman collection are currently incomplete — the weight tracking endpoints (from v1.9 Progress Tracking milestone) are missing entirely from API.md, and the Postman collection is missing Activity logging/history/summary/delete, Weekly Plans, Daily Meal Plans, Activity Plans, and Progress/Weight endpoints.

**Output:** Updated API.md with Section 10 (Progress), updated Postman collection with all missing endpoint groups.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@backend/docs/API.md
@docs/Fitness_App_API.postman_collection.json
@backend/src/routes/progress.routes.js
@backend/src/controllers/weightLog.controller.js
@backend/src/services/weightLog.service.js
@backend/src/routes/activity.routes.js
@backend/src/routes/weeklyPlan.routes.js
@backend/src/routes/dailyMealPlan.routes.js
@backend/src/routes/activityPlan.routes.js
@backend/src/middlewares/weeklyPlanRateLimiter.js
@backend/src/middlewares/dailyMealPlanRateLimiter.js
@backend/src/middlewares/activityPlanRateLimiter.js

**Actual rate limits (from code) vs what's currently documented:**
- Weekly Plan generate: 50 per 15 min (docs say 5) — UPDATE
- Weekly Plan regenerate-day: 30 per 30 min (docs say "separate limit") — UPDATE
- Weekly Plan swap: 10 per 5 min — UPDATE
- Weekly Plan toggle-complete: 60 per 1 min — UPDATE
- Daily Meal Plan generate: 50 per 15 min (docs say 5) — UPDATE
- Activity Plan generate: 50 per 15 min — ADD

**Progress/Weight endpoint details (from actual source):**
- POST /api/progress/weight — body: { weightKg (2-300), loggedDate (YYYY-MM-DD, required), notes (optional) }. Upserts. Also syncs weight to profile.
- GET /api/progress/weight?limit=N — returns entries sorted by date desc, default limit 50.
- DELETE /api/progress/weight/:id — deletes entry, checks ownership.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Section 10 (Progress) to API.md and fix rate limits</name>
  <files>backend/docs/API.md</files>
  <action>
    1. Add Progress rate limit row to the rate limit table: "Progress | 50 per 15 minutes | All /api/progress/ routes" — insert after "Activities" row.
    2. Update Weekly Plan generate rate limit from "5 per 15 minutes" to "50 per 15 minutes" in the rate limit table.
    3. Update Daily Meal Plan generate rate limit from "5 per 15 minutes" to "50 per 15 minutes" in the rate limit table.
    4. Add Activity Plan generate rate limit row: "Activity Plans Generate | 50 per 15 minutes | POST /api/activity-plans/generate"
    5. After the existing section 9 (Activity Plans), add a new Section 10:

    ### 10. Progress — `/api/progress`

    All progress endpoints require authentication. Rate limit: Progress (50 per 15 minutes).

    #### POST /api/progress/weight
    - Auth: Required
    - Rate Limit: Progress
    - Description: Log or update a weight entry for a specific date. Uses upsert — if a weight log already exists for the given date, it is updated. Also syncs the weight to the user's profile.
    - Constraints:
      - weightKg: Required, number between 2-300.
      - loggedDate: Required, format YYYY-MM-DD.
      - notes: Optional string.
    - Request Body:
      ```json
      { "weightKg": 70.5, "loggedDate": "2026-05-28", "notes": "Morning weigh-in" }
      ```
    - Response 201:
      ```json
      { "success": true, "data": { "entry": { "id": "uuid", "user_id": "uuid", "weight_kg": 70.5, "logged_date": "2026-05-28", "source": "manual", "notes": "Morning weigh-in" } } }
      ```
    - Error Codes: VALIDATION_ERROR (400), AUTHENTICATION_ERROR (401)

    #### GET /api/progress/weight
    - Auth: Required
    - Rate Limit: Progress
    - Description: Retrieve weight history entries sorted by date descending.
    - Query Parameters:
      - limit (optional): Maximum entries to return. Defaults to 50.
    - Response 200:
      ```json
      { "success": true, "data": { "entries": [ { "id": "uuid", "user_id": "uuid", "weight_kg": 70.5, "logged_date": "2026-05-28", "source": "manual", "notes": "Morning weigh-in" } ] } }
      ```
    - Error Codes: AUTHENTICATION_ERROR (401)

    #### DELETE /api/progress/weight/:id
    - Auth: Required
    - Rate Limit: Progress
    - Description: Delete a specific weight log entry. Only the owning user can delete their own logs.
    - Path Parameters:
      - id: UUID of the weight log entry.
    - Response 200:
      ```json
      { "success": true, "data": { "deleted": true } }
      ```
    - Error Codes: VALIDATION_ERROR (400) — invalid id, NOT_FOUND (404), AUTHENTICATION_ERROR (401)

    Use the same formatting style as existing sections (headers with "---" separators, consistent JSON formatting).
  </action>
  <verify>
    grep -c "### 10. Progress" backend/docs/API.md | ForEach-Object { $_ -gt 0 }
  </verify>
  <done>API.md has Section 10 with 3 Weight endpoints, rate limit table includes Progress with accurate values.</done>
</task>

<task type="auto">
  <name>Task 2: Add all missing endpoint groups to Postman collection</name>
  <files>docs/Fitness_App_API.postman_collection.json</files>
  <action>
    Parse the existing Postman collection JSON and add the following missing endpoint groups as new `item` entries in the root array, inserting in appropriate order:

    **Group 1: Missing Activity endpoints** (add after existing "Activity" group's `GET /api/activities` item):
    1. POST /api/activities/log — body: `{ "activityId": 1, "durationMin": 30, "intensity": "moderate", "loggedDate": "2026-05-28" }`, include test script checking Status 201 and calories_burned > 0.
    2. GET /api/activities/logs?date={{today}} — include test script checking Status 200 and returns array.
    3. GET /api/activities/history?days=7 — include test script checking Status 200 and returns array.
    4. DELETE /api/activities/log/:id — path param id=1, include test checking Status 200.
    5. GET /api/activities/summary?date={{today}} — include test script checking Status 200 and netCalories property.

    **Group 2: Weekly Plans** (new top-level group after Activity):
    1. POST /api/weekly-plans/generate — body: `{ "weekStart": "{{week_start}}" }`, test: Status 200.
    2. GET /api/weekly-plans?weekStart={{week_start}} — test: Status 200.
    3. POST /api/weekly-plans/regenerate-day — body: `{ "weekStart": "{{week_start}}", "dayIndex": 2 }`, test: Status 200.
    4. POST /api/weekly-plans/swap — body: `{ "weekStart": "{{week_start}}", "dayIndex": 2, "oldActivityName": "Brisk Walking", "intensity": "moderate" }`, test: Status 200.
    5. POST /api/weekly-plans/toggle-complete — body: `{ "weekStart": "{{week_start}}", "dayIndex": 2, "activityName": "Brisk Walking" }`, test: Status 200.

    **Group 3: Daily Meal Plans** (new top-level group after Weekly Plans):
    1. GET /api/daily-meal-plans?date={{today}} — test: Status 200.
    2. POST /api/daily-meal-plans/generate — body: `{ "date": "{{today}}" }`, test: Status 200.
    3. POST /api/daily-meal-plans/log — body: `{ "date": "{{today}}", "mealType": "lunch" }`, test: Status 200.
    4. POST /api/daily-meal-plans/toggle-item — body: `{ "date": "{{today}}", "mealType": "lunch", "itemIndex": 0 }`, test: Status 200.
    5. POST /api/daily-meal-plans/swap-item — body: `{ "date": "{{today}}", "mealType": "lunch", "itemIndex": 0 }`, test: Status 200.

    **Group 4: Activity Plans** (new top-level group after Daily Meal Plans):
    1. GET /api/activity-plans?date={{today}} — test: Status 200.
    2. POST /api/activity-plans/generate — body: `{ "date": "{{today}}" }`, test: Status 200.
    3. POST /api/activity-plans/log — body: `{ "date": "{{today}}" }`, test: Status 200.

    **Group 5: Progress/Weight** (new top-level group after Activity Plans):
    1. POST /api/progress/weight — body: `{ "weightKg": 70.5, "loggedDate": "2026-05-28" }`, test: Status 201, has entry.id.
    2. GET /api/progress/weight?limit=10 — test: Status 200, has entries array.
    3. DELETE /api/progress/weight/:id — path param id=1, test: Status 200, has deleted:true.

    Add a `week_start` variable to the collection's `variable` array:
    ```json
    { "key": "week_start", "value": "", "type": "string" }
    ```

    Follow the collection's existing JSON structure (pm.test scripts, request objects, Content-Type headers, variable references).

    Use proper JSON formatting — each request needs: method, header (Content-Type for POST), body (mode: 'raw' with JSON string), url (with host/path/query parts).
  </action>
  <verify>
    node -e "const c = require('./docs/Fitness_App_API.postman_collection.json'); const names = c.item.map(g => g.name); console.log('Groups:', names.join(', ')); process.exit(names.includes('Progress/Weight') ? 0 : 1)"
  </verify>
  <done>Postman collection includes all endpoint groups: Activity (8 items), Weekly Plans (5), Daily Meal Plans (5), Activity Plans (3), Progress/Weight (3).</done>
</task>

</tasks>

<verification>
1. `grep -c "### 10. Progress" backend/docs/API.md` — must be > 0
2. `grep -c "weight_kg" backend/docs/API.md` — must show weight_kg field documented
3. Postman collection node check reports Progress/Weight group exists
4. JSON is valid: `node -e "JSON.parse(fs.readFileSync('docs/Fitness_App_API.postman_collection.json','utf8'))"`
</verification>

<success_criteria>
- API.md has Section 10 with 3 weight tracking endpoints documented
- Rate limit table includes Progress entry and has accurate values for all LLM generation limits
- Postman collection has all 21 missing endpoint requests across 5 new groups
- Both files are valid (markdown renders correctly, JSON parses correctly)
</success_criteria>

<output>
After completion, create SUMMARY.md in this quick dir.
</output>
