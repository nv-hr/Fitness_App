---
status: complete
---

# Summary: Sync weight logs to profile weight

## Result
Successfully implemented bidirectional weight sync. When a user logs weight via the progress tracking feature (`POST /api/progress/weight`), the `profiles.weight_kg` field is now updated automatically.

## Changes
- `backend/src/repositories/profile.repository.js` — Added `updateWeightKg(userId, weightKg)` function that updates only the weight field
- `backend/src/services/weightLog.service.js` — After upserting weight log, calls `profileRepo.updateWeightKg()` with error-tolerant try/catch

## Verification
- Backend unit tests: 6/7 suites pass (1 pre-existing failure in llm.service.test.js)
- Profile service tests pass
