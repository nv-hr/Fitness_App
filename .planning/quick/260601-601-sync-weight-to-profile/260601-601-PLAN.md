# Quick Task 260601-601: Sync weight logs to profile weight

**Date:** 2026-06-01
**Status:** Complete

## Task 1: Add profile weight sync on weight log

- **Action:** Add `updateWeightKg(userId, weightKg)` to `profile.repository.js` and call it from `weightLog.service.js` after upserting a weight log entry
- **Files modified:** `backend/src/repositories/profile.repository.js`, `backend/src/services/weightLog.service.js`
- **Verification:** Backend unit tests pass; when weight is logged via POST `/api/progress/weight`, the `profiles.weight_kg` field is updated to match
