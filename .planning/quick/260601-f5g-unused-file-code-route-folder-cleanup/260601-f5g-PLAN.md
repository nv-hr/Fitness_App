---
phase: quick
plan: 260601-f5g
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
files_removed:
  - plan-phase-18/PLAN.md
  - plan-phase-19/PLAN.md
  - docs/BMI Calculator
  - docs/KCAL Calculator
  - docs/TDEE Calculator
  - docs/Workout Planner
  - docs/Workout Progress
  - docs/FRONTEND_ANALYSIS.md
  - backend/src/routes/mealPlan.routes.js
  - backend/src/controllers/mealPlan.controller.js
  - backend/src/middlewares/mealPlanRateLimiter.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "No stale planning artifact directories exist at project root"
    - "Vite cache directory is gitignored and removed from tracking"
    - "Old Bahasa Indonesia feature docs are removed"
    - "Orphaned v1.4 mealPlan backend files (route, controller, middleware) are removed"
    - "Tests still pass after all removals"
  artifacts:
    - path: ".gitignore"
      provides: ".vite directory excluded from git tracking"
      contains: ".vite"
  key_links:
    - from: "backend/src/app.js"
      to: "backend/src/routes/*.routes.js"
      pattern: "No reference to mealPlan.routes.js (already absent)"
---

<objective>
Remove orphaned and stale files from the codebase — old planning artifact directories, stale feature docs, Vite cache, and orphaned v1.4 backend files (mealPlan route/controller/middleware) that were superseded by the daily meal plan system in v1.5.

Purpose: Housekeeping — reduce noise, prevent dead code confusion, keep .gitignore up to date.
Output: Cleaner project root, one fewer orphaned backend subsystem, .gitignore updated.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
These items have been identified during codebase exploration:

**Stale root-level directories:**
- `plan-phase-18/` — leftover artifact from v1.4 (one PLAN.md)
- `plan-phase-19/` — same, from v1.4
- `.vite/` — Vite dependency cache, 2 files (169 bytes), not in `.gitignore`

**Stale docs/ files (superseded by PROJECT.md, README.md, codebase docs):**
- `docs/BMI Calculator` — 3-line Bahasa Indonesia feature description
- `docs/KCAL Calculator` — same
- `docs/TDEE Calculator` — same
- `docs/Workout Planner` — same
- `docs/Workout Progress` — same
- `docs/FRONTEND_ANALYSIS.md` — v1.2 analysis document, completely stale

**Orphaned backend files (v1.4 legacy weekly meal plan system superseded by v1.5 daily meal plan):**
- `backend/src/routes/mealPlan.routes.js` — NOT imported in `app.js` (zero references from active code)
- `backend/src/controllers/mealPlan.controller.js` — only imported by orphaned route above
- `backend/src/middlewares/mealPlanRateLimiter.js` — only imported by orphaned route above

**KEPT intentionally (still in use):**
- `docs/Fitness_App_API.postman_collection.json` — still useful for API testing
- `backend/src/services/mealPlan.service.js` — `dailyMealPlan.service.js` imports `fuzzyMatchFoodName` and `recalculateDayCalories` from it
- `backend/src/repositories/mealPlan.repository.js` — `mealPlan.service.js` depends on it (indirectly used)
- `frontend/src/features/weekly-plan/` — `DayActivityRow.jsx` and `Toast.jsx` still imported by `ActivityCalendarSection.jsx`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove stale root-level directories and update .gitignore</name>
  <files>
    plan-phase-18/PLAN.md
    plan-phase-19/PLAN.md
    .vite/
    .gitignore
  </files>
  <action>
    Remove two stale planning artifact directories and the Vite cache directory at project root:

    1. Delete `plan-phase-18/` recursively:
       `Remove-Item -Recurse -Force "plan-phase-18"`

    2. Delete `plan-phase-19/` recursively:
       `Remove-Item -Recurse -Force "plan-phase-19"`

    3. Delete `.vite/` recursively:
       `Remove-Item -Recurse -Force ".vite"`

    4. Add `.vite` to `.gitignore` — append the line `.vite` on a new line at the end of the file (after the existing entries). Use a newline before it if the file doesn't end with one. Do NOT remove any existing entries.
  </action>
  <verify>
    <automated>
      # Verify directories removed
      if (Test-Path "plan-phase-18") { Write-Error "plan-phase-18 still exists" }
      if (Test-Path "plan-phase-19") { Write-Error "plan-phase-19 still exists" }
      if (Test-Path ".vite") { Write-Error ".vite still exists" }
      # Verify .vite in gitignore
      Select-String -Pattern "^\.vite$" -Path ".gitignore" -SimpleMatch | Out-String
    </automated>
  </verify>
  <done>
    plan-phase-18/, plan-phase-19/, .vite/ directories removed from filesystem; .gitignore updated with .vite entry
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove stale docs/ files</name>
  <files>
    docs/BMI Calculator
    docs/KCAL Calculator
    docs/TDEE Calculator
    docs/Workout Planner
    docs/Workout Progress
    docs/FRONTEND_ANALYSIS.md
  </files>
  <action>
    Remove 6 stale feature description files from the docs/ directory. These are early-project feature descriptions in Bahasa Indonesia (except FRONTEND_ANALYSIS.md which is a v1.2-era analysis doc). All are completely superseded by PROJECT.md, README.md, and the actual codebase.

    KEEP: `docs/Fitness_App_API.postman_collection.json` — still useful for manual API testing.

    1. `Remove-Item -Force "docs/BMI Calculator"`
    2. `Remove-Item -Force "docs/KCAL Calculator"`
    3. `Remove-Item -Force "docs/TDEE Calculator"`
    4. `Remove-Item -Force "docs/Workout Planner"`
    5. `Remove-Item -Force "docs/Workout Progress"`
    6. `Remove-Item -Force "docs/FRONTEND_ANALYSIS.md"`

    Verify that `docs/Fitness_App_API.postman_collection.json` is NOT deleted.
  </action>
  <verify>
    <automated>
      # Verify stale files removed
      $staleFiles = @("docs/BMI Calculator", "docs/KCAL Calculator", "docs/TDEE Calculator", "docs/Workout Planner", "docs/Workout Progress", "docs/FRONTEND_ANALYSIS.md")
      $missing = $true
      foreach ($f in $staleFiles) { if (Test-Path $f) { Write-Error "$f still exists"; $missing = $false } }
      if ($missing) { Write-Output "All stale docs removed" }
      # Verify postman collection preserved
      if (Test-Path "docs/Fitness_App_API.postman_collection.json") { Write-Output "Postman collection preserved" }
      else { Write-Error "Postman collection was accidentally deleted" }
    </automated>
  </verify>
  <done>
    6 stale docs files removed; Fitness_App_API.postman_collection.json preserved
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove orphaned v1.4 backend files (mealPlan route/controller/middleware)</name>
  <files>
    backend/src/routes/mealPlan.routes.js
    backend/src/controllers/mealPlan.controller.js
    backend/src/middlewares/mealPlanRateLimiter.js
  </files>
  <action>
    Remove 3 orphaned backend files from the v1.4 weekly meal plan system. These were superseded by the daily meal plan system in v1.5.

    - `backend/src/routes/mealPlan.routes.js` — NOT imported in `app.js` (zero active references)
    - `backend/src/controllers/mealPlan.controller.js` — only imported by the orphaned route above
    - `backend/src/middlewares/mealPlanRateLimiter.js` — only imported by the orphaned route above; its rate limiters (`mealPlanLimiter`, `regenerateLimiter`, `logDayLimiter`) have zero external references in active code

    DO NOT remove:
    - `backend/src/services/mealPlan.service.js` — still used by `dailyMealPlan.service.js` (exports `fuzzyMatchFoodName`, `recalculateDayCalories`)
    - `backend/src/repositories/mealPlan.repository.js` — still used by `mealPlan.service.js`

    1. `Remove-Item -Force "backend/src/routes/mealPlan.routes.js"`
    2. `Remove-Item -Force "backend/src/controllers/mealPlan.controller.js"`
    3. `Remove-Item -Force "backend/src/middlewares/mealPlanRateLimiter.js"`
  </action>
  <verify>
    <automated>
      # Verify orphaned files removed
      if (Test-Path "backend/src/routes/mealPlan.routes.js") { Write-Error "mealPlan.routes.js still exists" }
      if (Test-Path "backend/src/controllers/mealPlan.controller.js") { Write-Error "mealPlan.controller.js still exists" }
      if (Test-Path "backend/src/middlewares/mealPlanRateLimiter.js") { Write-Error "mealPlanRateLimiter.js still exists" }
      # Verify kept files still exist
      if (-not (Test-Path "backend/src/services/mealPlan.service.js")) { Write-Error "mealPlan.service.js was deleted (should keep)" }
      if (-not (Test-Path "backend/src/repositories/mealPlan.repository.js")) { Write-Error "mealPlan.repository.js was deleted (should keep)" }
      Write-Output "Orphaned files removed, supporting services preserved"
    </automated>
  </verify>
  <done>
    3 orphaned backend files removed; supporting services/repositories preserved; full test suite passes
  </done>
</task>

</tasks>

<verification>
After all tasks complete, run the following to confirm nothing is broken:

```powershell
# 1. Verify backend app.js has no import errors for removed files
node -e "require('./backend/src/app.js')" 2>&1 | Out-Null
if ($?) { Write-Output "Backend app.js loads without errors" }

# 2. Run backend unit tests (excludes integration which needs DB)
cd backend; npx jest --testPathPattern='tests/unit' --silent 2>&1; cd ..
```

Note: The removed files had zero active imports — `mealPlan.routes.js` was never imported in `app.js`, `mealPlan.controller.js` only imported by that unregistered route, and `mealPlanRateLimiter.js` only imported by that route. Removal is mechanically safe.
</verification>

<success_criteria>
- [ ] plan-phase-18/, plan-phase-19/, .vite/ directories removed
- [ ] .gitignore updated with `.vite` entry
- [ ] 6 stale docs files removed, Postman collection preserved
- [ ] 3 orphaned backend files removed, supporting services preserved
- [ ] Backend unit tests still pass
- [ ] Backend app.js loads without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260601-f5g-unused-file-code-route-folder-cleanup/260601-f5g-SUMMARY.md`
</output>
