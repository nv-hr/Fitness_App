# Quick Task 260528-849: Fix SPA catch-all ENOENT on backend "/" routes

**Description:** Visiting `GET /` on the backend (port 3001) in dev mode returns a 500 with `ENOENT: no such file or directory, stat '.../backend/public/index.html'`. The SPA catch-all middleware calls `res.sendFile('index.html')` unconditionally, but `public/` only exists in Docker production builds.

**Tasks:**

1. Add `fs.existsSync` guard in SPA catch-all middleware — skip to 404 handler if `public/index.html` doesn't exist
