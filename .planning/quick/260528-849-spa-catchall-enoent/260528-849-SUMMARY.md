---
status: complete
---

# Quick Task 260528-849: Fix SPA catch-all ENOENT on backend "/" routes

**Status:** Complete

## Fix Applied

Added `fs.existsSync('public/index.html')` guard in `backend/src/app.js` SPA catch-all middleware. If the frontend build doesn't exist (dev mode), the middleware calls `next()` to pass through to the 404 handler which returns a proper JSON `{ success: false, error: { code: 'NOT_FOUND' } }` instead of crashing with ENOENT.

## Files Modified
- `backend/src/app.js` — Added `fs` import and `fs.existsSync` guard in SPA catch-all

## Verification
- All 81 backend tests pass (6 test suites)
