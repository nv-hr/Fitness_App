# Quick Task 260528-l3x: Delete supabase/migrations/

**Date:** 2026-05-28

## Task

1. Delete `supabase/migrations/` directory (schema already applied to Supabase cloud DB)
2. Keep `supabase/config.toml` and `supabase/.gitignore` intact

## Files
- `supabase/migrations/schema.sql` — delete
- `supabase/migrations/` — delete directory

## Action
`git rm -r supabase/migrations/`

## Verify
`supabase/migrations/` no longer exists on disk or in git tracking

## Done
Commit with message: `chore: remove supabase/migrations/ (schema already in cloud DB)`
