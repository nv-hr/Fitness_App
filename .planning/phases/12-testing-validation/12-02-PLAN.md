---
phase: 12-testing-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/.env
  - backend/src/config/database.js
  - frontend/src/__tests__/api-integration.test.js
autonomous: true
requirements:
  - TST-01

must_haves:
  truths:
    - "DATABASE_URL_TEST env var is documented and supported"
    - "database.js reads DATABASE_URL with fallback to env default"
    - "Frontend API integration test assertions match PostgreSQL boolean types"
  artifacts:
    - path: "backend/.env"
      provides: "DATABASE_URL_TEST configuration variable"
      contains: "DATABASE_URL_TEST"
    - path: "frontend/src/__tests__/api-integration.test.js"
      provides: "Updated assertions for PostgreSQL booleans"
      contains: "is_custom: true"
  key_links:
    - from: "database.js"
      to: ".env"
      via: "dotenv loads .env, pool reads DATABASE_URL from process.env"
      pattern: "dotenv"
    - from: "api-integration.test.js"
      to: "backend server"
      via: "fork() child process for backend"
      pattern: "fork"
---

<objective>
Add `DATABASE_URL_TEST` support to the backend configuration so integration tests can target a separate test schema. Update the frontend API integration test assertions for PostgreSQL boolean types.

Purpose: Enable the testing infrastructure to use an isolated test schema while keeping production DATABASE_URL unchanged. Ensure frontend test assertions match PostgreSQL data types.
Output: Frontend tests passing with `vitest run` after assertion fixes, .env and database.js supporting DATABASE_URL_TEST.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-testing-validation/12-CONTEXT.md
@.planning/phases/12-testing-validation/12-01-PLAN.md

<interfaces>
From backend/src/config/database.js (current):
```javascript
import { Pool } from 'pg';
import dotenv from 'dotenv';
dotenv.config();

export const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  ssl: { rejectUnauthorized: true },
  max: 10,
  connectionTimeoutMillis: 5000,
  idleTimeoutMillis: 30000,
});
```

From backend/.env (current):
```
# Database (Supabase PostgreSQL)
JWT_SECRET=8f3d2b7c1a9e4f6d8b0c2e5f7a1d3c6e9f2b4a7d1c5e8f0b3a6d9c2e4f7a1b5
GOOGLE_CLIENT_ID=589754502197-...
GOOGLE_CLIENT_SECRET=GOCSPX-...
DATABASE_URL=postgresql://postgres:niliterna86gt@db.qddyfkmzjmuhhknbyxwt.supabase.co:5432/postgres
```

From frontend/src/__tests__/api-integration.test.js (current assertion to fix — line ~426):
```javascript
data: { name: 'Test Custom Meal', is_custom: 1 },
```

Expected PostgreSQL pattern:
```javascript
data: { name: 'Test Custom Meal', is_custom: true },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DATABASE_URL_TEST to .env and database.js</name>
  <files>backend/.env, backend/src/config/database.js</files>

  <read_first>
    - backend/.env (current env vars)
    - backend/src/config/database.js (pg Pool config)
    - .planning/phases/12-testing-validation/12-CONTEXT.md (D-02)
  </read_first>

  <action>
    Per D-02, add `DATABASE_URL_TEST` support so integration tests can target a separate test schema.

    **1. Add to `backend/.env`:**
    Append after the existing `DATABASE_URL` line:
    ```env
    # Integration test schema (same Supabase project, fitness_test schema — per D-01)
    DATABASE_URL_TEST=postgresql://postgres:niliterna86gt@db.qddyfkmzjmuhhknbyxwt.supabase.co:5432/postgres?options=-c%20search_path%3Dfitness_test
    ```
    
    Note: The value is intentionally identical to DATABASE_URL with `?options=-c%20search_path%3Dfitness_test` appended. This uses PostgreSQL connection options to set the search path. However, per D-02, the helpers.js actually overrides `process.env.DATABASE_URL` at runtime with this value and manages schema via SQL commands. The `.env` entry serves as documentation and default.

    Write a descriptive comment above it explaining its purpose as shown above.

    **2. Modify `backend/src/config/database.js`:**
    No code changes needed — the pool already reads `process.env.DATABASE_URL` from the environment. The helpers.js in Plan 12-01 overrides `process.env.DATABASE_URL` with `DATABASE_URL_TEST` before the app module is imported.

    However, add a comment block documenting the DATABASE_URL_TEST convention:
    ```javascript
    // DATABASE_URL_TEST: Optional env var for integration test schema isolation.
    // When set, helpers.js overrides DATABASE_URL with DATABASE_URL_TEST
    // before importing the app. Tests run against 'fitness_test' schema.
    // See: .planning/phases/12-testing-validation/12-CONTEXT.md (D-02)
    ```

    Place this comment right before the `export const pool = new Pool({...})` line.
  </action>

  <verify>
    <automated># Verify .env has DATABASE_URL_TEST
if (Select-String -Path "backend/.env" -Pattern "DATABASE_URL_TEST" -Quiet) { Write-Host "✓ DATABASE_URL_TEST in .env" } else { Write-Error "FAIL: DATABASE_URL_TEST missing from .env"; exit 1 }

# Verify database.js has documentation comment
if (Select-String -Path "backend/src/config/database.js" -Pattern "DATABASE_URL_TEST" -Quiet) { Write-Host "✓ database.js documents DATABASE_URL_TEST" } else { Write-Warning "⚠ DATABASE_URL_TEST doc comment not found" }</automated>
  </verify>

  <acceptance_criteria>
    1. `backend/.env` contains `DATABASE_URL_TEST=` with descriptive comment
    2. `backend/src/config/database.js` contains documentation comment about DATABASE_URL_TEST
    3. No functional changes to database.js — pool config remains identical
  </acceptance_criteria>
  <done>1. .env has DATABASE_URL_TEST 2. database.js documents DATABASE_URL_TEST 3. No functional changes to pool config</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend API integration test for PostgreSQL booleans</name>
  <files>frontend/src/__tests__/api-integration.test.js</files>

  <read_first>
    - frontend/src/__tests__/api-integration.test.js (current 601-line file)
  </read_first>

  <action>
    Make minimal targeted assertion changes in the frontend test file to fix MySQL-specific patterns (per D-06).

    **1. Fix `is_custom` boolean value (line ~426):**
    - Change: `data: { name: 'Test Custom Meal', is_custom: 1 },`
    - To: `data: { name: 'Test Custom Meal', is_custom: true },`
    - Rationale: PostgreSQL BOOLEAN returns `true`/`false`, not `1`/`0`

    **2. Search for any other MySQL-specific patterns:**
    - Search the file for `.toBe(1)` or `.toBe(0)` — these should only be for array length checks (unchanged)
    - Search for `is_custom` — only the one occurrence at ~line 426 should exist
    - Search for `mysql` — should not exist in the entire file

    **3. No other changes needed** per D-06:
    - The frontend test forks backend as child process — no helpers rewrite needed
    - Backend connects via DATABASE_URL (which helpers.js overrides for test schema)
    - All test structure, endpoint coverage, and unique email patterns remain unchanged
    - Cookie extraction and API call patterns remain unchanged
  </action>

  <verify>
    <automated># Check is_custom uses true not 1
if (Select-String -Path "frontend/src/__tests__/api-integration.test.js" -Pattern "is_custom: 1" -Quiet) { Write-Error "FAIL: is_custom: 1 still present"; exit 1 } else { Write-Host "✓ is_custom: 1 removed" }

if (Select-String -Path "frontend/src/__tests__/api-integration.test.js" -Pattern "is_custom: true" -Quiet) { Write-Host "✓ is_custom: true present" } else { Write-Warning "⚠ is_custom: true not found" }

# Check no mysql references
if (Select-String -Path "frontend/src/__tests__/api-integration.test.js" -Pattern "mysql" -Quiet) { Write-Warning "⚠ mysql reference found" } else { Write-Host "✓ No mysql references" }

# Verify total test line count preserved (within 5 lines)
$before = 601; $after = (Get-Content "frontend/src/__tests__/api-integration.test.js" | Measure-Object -Line).Lines
$diff = [Math]::Abs($after - $before)
if ($diff -le 5) { Write-Host "✓ File size stable ($before → $after lines)" } else { Write-Warning "⚠ File changed by $diff lines" }</automated>
  </verify>

  <acceptance_criteria>
    1. `frontend/src/__tests__/api-integration.test.js` has `is_custom: true` instead of `is_custom: 1`
    2. No MySQL-specific patterns remain in the file
    3. Test file length approximately unchanged (601 lines ± 5)
    4. All test cases preserved with original structure
  </acceptance_criteria>
  <done>1. Frontend test has is_custom: true not is_custom: 1 2. No mysql references 3. File length stable (~601 lines)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file ↔ Git repository | Test database credentials must not be committed |
| Test schema ↔ Public schema | DATABASE_URL_TEST targets fitness_test schema, protecting production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | backend/.env (DATABASE_URL_TEST) | mitigate | .env already in .gitignore; add DATABASE_URL_TEST to .env.example with placeholder value |
| T-12-05 | Spoofing | Frontend test child process | accept | Frontend test forks backend with NODE_ENV=test — acceptable risk for test environment |
</threat_model>

<verification>
1. `Select-String -Path "backend/.env" -Pattern "DATABASE_URL_TEST"` — env var exists
2. `Select-String -Path "frontend/src/__tests__/api-integration.test.js" -Pattern "is_custom: true"` — assertion updated
3. `Select-String -Path "frontend/src/__tests__/api-integration.test.js" -Pattern "is_custom: 1" -Quiet` — old pattern absent
4. `(Get-Content "backend/src/config/database.js" | Select-String "DATABASE_URL_TEST")` — doc comment present
</verification>

<success_criteria>
1. backend/.env has DATABASE_URL_TEST documented with descriptive comment
2. backend/src/config/database.js has DATABASE_URL_TEST convention documentation
3. frontend test has is_custom: true (not is_custom: 1)
4. No functional changes to database.js pool configuration
</success_criteria>

<output>
After completion, create `.planning/phases/12-testing-validation/12-02-SUMMARY.md`
</output>
