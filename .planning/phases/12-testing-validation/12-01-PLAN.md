---
phase: 12-testing-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/helpers.js
  - backend/tests/integration/api.test.js
autonomous: true
requirements:
  - TST-01

must_haves:
  truths:
    - "Backend integration tests start up successfully against Supabase test schema"
    - "helpers.js manages test schema lifecycle (create, seed, clean) without MySQL"
    - "All 31 existing test cases pass against PostgreSQL with correct boolean assertions"
    - "Custom food `is_custom` field returns `true` (PostgreSQL BOOLEAN) not `1` (MySQL TINYINT)"
  artifacts:
    - path: "backend/tests/integration/helpers.js"
      provides: "Supabase test schema lifecycle: create, seed, clean"
      min_lines: 150
    - path: "backend/tests/integration/api.test.js"
      provides: "Updated test assertions for PostgreSQL boolean types"
      contains: "toBe(true)"
  key_links:
    - from: "helpers.js"
      to: "database.js (pg Pool)"
      via: "import pool, run seed SQL on test schema"
      pattern: "pool\\.query"
    - from: "helpers.js"
      to: "schema.sql + seed.sql"
      via: "read SQL files and execute against test schema"
      pattern: "seed\\.sql|schema\\.sql"
    - from: "api.test.js"
      to: "helpers.js"
      via: "imports start/stop helpers"
      pattern: "startDatabase|stopDatabase"
---

<objective>
Migrate the backend integration test suite from MySQL to Supabase PostgreSQL. Rewrite `helpers.js` to manage a test schema (`fitness_test`) in the existing Supabase project instead of starting/stopping a MySQL container. Update MySQL-specific assertions in `api.test.js` for PostgreSQL boolean types.

Purpose: Make the existing 774-line test suite (31 test cases across 16 endpoints) run against Supabase PostgreSQL, validating the migration is complete.
Output: Working integration test suite that passes with `node --experimental-vm-modules node_modules/jest/bin/jest.js --testPathPattern=integration`
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-testing-validation/12-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From backend/src/config/database.js:
```javascript
import { Pool } from 'pg';
import dotenv from 'dotenv';
dotenv.config();
export const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  ssl: { rejectUnauthorized: true },
  max: 10,
  connectionTimeoutMillis: 5000,
  idleTimeoutMillis: 30000,
});
```

From existing helpers.js (exported functions to preserve):
- `createTestUser(agent)` → creates user via API, returns `{agent, email, password, user}`
- `seedTestData(agent)` → creates profile + food log, returns `{profile, foodLogId}`

From backend/src/utils/dbErrors.js:
```javascript
export function normalizeDbError(err) {
  const CODE_MAP = {
    '23505': 'UNIQUE_VIOLATION', '23503': 'FOREIGN_KEY_VIOLATION',
    '23502': 'NOT_NULL_VIOLATION', '23514': 'CHECK_VIOLATION',
  };
  return { code: CODE_MAP[err.code] || err.code || 'UNKNOWN_DATABASE_ERROR', ... };
}
```

Supabase test schema SQL patterns:
```sql
-- Create/clean test schema
DROP SCHEMA IF EXISTS fitness_test CASCADE;
CREATE SCHEMA fitness_test;
-- Run schema.sql with search_path = fitness_test
-- Run seed.sql with search_path = fitness_test
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite helpers.js for Supabase test schema lifecycle</name>
  <files>backend/tests/integration/helpers.js</files>

  <read_first>
    - backend/tests/integration/helpers.js (current MySQL version — 223 lines)
    - backend/src/config/database.js (pg Pool export)
    - backend/.env (DATABASE_URL, target for DATABASE_URL_TEST)
  </read_first>

  <action>
    Replace the entire `helpers.js` file. Keep ALL existing exports (`createTestUser`, `seedTestData`) unchanged — they work via API calls and don't depend on MySQL.

    **Replace `startDatabase(timeoutMs)` and `stopDatabase()`** with Supabase test schema lifecycle:

    1. **Import changes:**
       - Remove: `execSync`, `createConnection` from `net`, `existsSync` from `fs`
       - Keep: `setTimeout` from `timers/promises`
       - Add: `import { readFileSync } from 'fs'`, `import { fileURLToPath } from 'url'`, `import path from 'path'`
       - Add: `import { pool } from '../../src/config/database.js'` — import the existing pg Pool

    2. **New `startDatabase(timeoutMs = 30000)`:**
       ```
       a. Read DATABASE_URL_TEST from process.env (per D-02). Fall back to DATABASE_URL if TEST not set.
       b. Create a temporary pool with the TEST connection string + ssl:{rejectUnauthorized:true} + max:1.
         ONLY for schema management — the app's own pool connects via DATABASE_URL as normal.
       c. DROP SCHEMA IF EXISTS fitness_test CASCADE (fresh isolation per run — D-04 discretion).
         Use a try/catch — the schema may not exist on first run.
       d. CREATE SCHEMA fitness_test.
       e. Read backend/config/schema.sql and backend/config/seed.sql using readFileSync with UTF-8.
       f. Execute schema.sql against the test schema:
          - Prepend `SET search_path TO fitness_test;` before the SQL content.
          - Use `tempPool.query(sql)` to run it.
          - Split on semicolons if needed for multi-statement execution, or use a single query with the `SET search_path` prefix.
       g. Execute seed.sql with the same approach.
       h. Close the temporary pool.
       i. Set `process.env.DATABASE_URL` to the test connection string — the app's pool reads this at import time.
          IMPORTANT: This must be set BEFORE the test file imports `app`. The current helpers.js sets
          `process.env.NODE_ENV = 'test'` at the top of the module — add `DATABASE_URL` override there too.
       j. Return (ready for tests).
       ```

    3. **New `stopDatabase()`:**
       ```
       a. Create a temporary pool with DATABASE_URL_TEST (same pattern as startDatabase).
       b. DROP SCHEMA IF EXISTS fitness_test CASCADE.
       c. Close the temporary pool.
       d. Note: The app's pool stays alive for the test process lifetime — Jest handles cleanup.
       ```

    4. **Imports at top of file — add DATABASE_URL override IMMEDIATELY after `process.env.NODE_ENV = 'test'`:**
       ```javascript
       // Set test environment before app imports
       process.env.NODE_ENV = 'test';
       
       // Use DATABASE_URL_TEST for integration tests (per D-02)
       if (process.env.DATABASE_URL_TEST) {
         process.env.DATABASE_URL = process.env.DATABASE_URL_TEST;
       }
       ```

    5. **Remove ALL MySQL-related code:**
       - `isPortOpen()` function (MySQL port check)
       - `composeCmd` variable
       - All docker-compose detection and execution code
       - `PROJECT_ROOT` and `COMPOSE_FILE` constants
       - Any `docker compose`, `docker-compose`, `podman-compose` references

    6. **Keep unchanged:**
       - `createTestUser(agent)` — works via API, no MySQL dependency
       - `seedTestData(agent)` — works via API, no MySQL dependency
       - All JSDoc comments and module-level documentation

    **Per D-04:** Rewrite only the MySQL lifecycle parts. Keep the test data seeding logic.
    **Per D-03:** Test schema populated by running seed SQL on the test schema before test suite.
  </action>

  <verify>
    <automated>node -e "
import('./backend/tests/integration/helpers.js').then(m => {
  const exports = Object.keys(m);
  const required = ['createTestUser', 'seedTestData', 'startDatabase', 'stopDatabase'];
  const missing = required.filter(r => !exports.includes(r));
  if (missing.length) { console.error('Missing exports:', missing); process.exit(1); }
  // Verify MySQL code removed
  const fs = require('fs');
  const src = fs.readFileSync('./backend/tests/integration/helpers.js', 'utf8');
  const forbidden = ['createConnection', 'isPortOpen', 'composeCmd', 'COMPOSE_FILE', 'podman-compose'];
  const found = forbidden.filter(f => src.includes(f));
  if (found.length) { console.error('MySQL code still present:', found); process.exit(1); }
  console.log('✓ helpers.js exports verified, MySQL code removed');
  // Verify pg Pool import
  if (!src.includes('database.js') && !src.includes('pg')) console.warn('⚠ No pg Pool import found');
  if (src.includes('search_path') || src.includes('DROP SCHEMA')) console.log('✓ Test schema management found');
}).catch(e => { console.error('Import error:', e.message); process.exit(1); });
"</automated>
  </verify>

  <acceptance_criteria>
    1. `backend/tests/integration/helpers.js` exports `startDatabase`, `stopDatabase`, `createTestUser`, `seedTestData`
    2. No MySQL code remains: no `isPortOpen`, `composeCmd`, `createConnection`, `COMPOSE_FILE`
    3. File imports from `../../src/config/database.js` or has pg Pool import
    4. File contains `DROP SCHEMA IF EXISTS fitness_test CASCADE` for test isolation
    5. File contains `process.env.DATABASE_URL = process.env.DATABASE_URL_TEST` for connection override
    6. File contains `schema.sql` and `seed.sql` read/evaluate logic
  </acceptance_criteria>
  <done>1. helpers.js exports all 4 functions 2. No MySQL code remains 3. Contains DROP SCHEMA + seed SQL logic 4. Contains DATABASE_URL override</done>
</task>

<task type="auto">
  <name>Task 2: Update api.test.js assertions for PostgreSQL boolean types</name>
  <files>backend/tests/integration/api.test.js</files>

  <read_first>
    - backend/tests/integration/api.test.js (current MySQL version — 774 lines)
    - backend/tests/integration/helpers.js (rewritten in Task 1)
  </read_first>

  <action>
    Make minimal targeted assertion changes in `api.test.js` to fix MySQL-specific assertions. The test structure, endpoint coverage, and all 31 test cases remain unchanged.

    1. **Fix boolean assertion (line 453):**
       - Change: `expect(res.body.data.is_custom).toBe(1);`
       - To: `expect(res.body.data.is_custom).toBe(true);`
       - Rationale (per D-05): MySQL stored booleans as TINYINT (0/1), PostgreSQL uses native BOOLEAN (true/false)

    2. **Verify no other MySQL-specific assertions exist:**
       - Search for `.toBe(1)` — only the `is_custom` case above should exist
       - Search for `.toBe(0)` — these remain valid (e.g., array length checks, `res.body.data.length`)
       - Search for `ER_DUP_ENTRY` — should not exist in this file (it's in the controller error mapper)
       - Search for `mysql` — should not exist in this file

    3. **No other changes needed** per D-04/D-05:
       - DECIMAL/NUMERIC fields (like `weight_kg: '70.00'`) still return strings from pg driver — assertions unchanged
       - All test case names, endpoint coverage, and structure remain identical
       - All unique email patterns (timestamp + random) remain unchanged
       - All SuperTest agent patterns remain unchanged

    4. **Verify `beforeAll` timeout** — the existing 120000ms timeout for `beforeAll` may be excessive
       now that we don't need to wait for MySQL. Reduce to 60000ms (faster schema setup).
       - Change: `}, 120000); // 2-minute timeout for DB startup`
       - To: `}, 60000); // 60s timeout for test schema setup`
  </action>

  <verify>
    <automated># Check boolean assertion is updated
if (Select-String -Path "backend/tests/integration/api.test.js" -Pattern "is_custom.*toBe\(1\)" -Quiet) { Write-Error "FAIL: is_custom still uses .toBe(1)"; exit 1 } else { Write-Host "✓ is_custom assertion updated" }

# Confirm we have the right value
if (Select-String -Path "backend/tests/integration/api.test.js" -Pattern "is_custom.*toBe\(true\)" -Quiet) { Write-Host "✓ is_custom uses .toBe(true)" } else { Write-Warning "⚠ is_custom .toBe(true) not found" }

# Check timeout reduced
if (Select-String -Path "backend/tests/integration/api.test.js" -Pattern "60000.*test schema" -Quiet) { Write-Host "✓ Timeout reduced to 60s" } else { Write-Warning "⚠ Timeout may still be 120s" }

# Verify no mysql references
if (Select-String -Path "backend/tests/integration/api.test.js" -Pattern "mysql" -Quiet) { Write-Warning "⚠ mysql reference found" } else { Write-Host "✓ No mysql references" }</automated>
  </verify>

  <acceptance_criteria>
    1. `backend/tests/integration/api.test.js` has `expect(res.body.data.is_custom).toBe(true)` instead of `.toBe(1)`
    2. No MySQL-specific patterns remain in the test file
    3. beforeAll timeout reduced from 120000ms to 60000ms
    4. All 31 test cases preserved with original structure
  </acceptance_criteria>
  <done>1. is_custom assertion uses toBe(true) not toBe(1) 2. No mysql references in file 3. beforeAll timeout reduced to 60s 4. All 31 test cases preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test schema ↔ Production schema | Test SQL runs in same Supabase project. Schema isolation is the only boundary. |
| .env file ↔ Version control | DATABASE_URL_TEST must not be committed to git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | backend/.env (DATABASE_URL_TEST) | mitigate | Ensure .env is in .gitignore; test DATABASE_URL_TEST from env, not hardcoded |
| T-12-02 | Tampering | helpers.js (DROP SCHEMA) | mitigate | Use SCHEMA-level isolation only (D-01). Never DROP or modify public schema tables. Test schema operations scoped to `fitness_test` schema. |
| T-12-03 | Elevation of Privilege | Temporary pg Pool in helpers | accept | Temporary pool created with same Supabase credentials as app. Test schema has restricted scope. Acceptable risk for testing. |
</threat_model>

<verification>
1. `node -e "import('./backend/tests/integration/helpers.js').then(m => console.log(Object.keys(m)))"` — all exports present
2. `Select-String -Path "backend/tests/integration/api.test.js" -Pattern "is_custom.*toBe\(true\)"` — assertion updated
3. `Select-String -Path "backend/tests/integration/helpers.js" -Pattern "createConnection|composeCmd|COMPOSE_FILE"` — no MySQL code
4. `Select-String -Path "backend/tests/integration/helpers.js" -Pattern "DROP SCHEMA.*fitness_test"` — schema isolation present
</verification>

<success_criteria>
1. helpers.js has 0 references to MySQL, docker-compose, or port checking
2. helpers.js exports startDatabase, stopDatabase, createTestUser, seedTestData
3. api.test.js has is_custom: true assertion (not toBe(1))
4. Integration test suite structurally unchanged — same endpoint coverage, same test cases
</success_criteria>

<output>
After completion, create `.planning/phases/12-testing-validation/12-01-SUMMARY.md`
</output>
