---
phase: 16-weekly-plan-frontend
plan: 03
type: execute
wave: 2
depends_on:
  - 16-02
files_modified:
  - frontend/src/app/Router.jsx
autonomous: true
requirements:
  - LLM-02
  - LLM-03

must_haves:
  truths:
    - "User can navigate to /weekly-plan route"
    - "User can access the weekly plan page from the dashboard via a nav link"
    - "Weekly plan route is protected behind authentication"
    - "WeeklyPlanPage component renders within ResponsiveLayout"
  artifacts:
    - path: "frontend/src/app/Router.jsx"
      provides: "Route registration and dashboard nav link"
      added_lines: "import WeeklyPlanPage, route definition, dashboard nav link"
  key_links:
    - from: "Router.jsx"
      to: "WeeklyPlanPage.jsx"
      via: "import"
      pattern: "import.*WeeklyPlanPage"
    - from: "DashboardPlaceholder"
      to: "/weekly-plan"
      via: "Link component"
      pattern: "to=\"/weekly-plan\""
---

<objective>
Register the `/weekly-plan` route in the frontend router and add a navigation link on the dashboard page.

Purpose: Users can navigate to the Weekly Plan page either by direct URL or from the main dashboard navigation links (per D-01).
Output: Modified `Router.jsx` with one new route import, one new route element, and one new dashboard nav link.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-weekly-plan-frontend/16-CONTEXT.md
@.planning/phases/16-weekly-plan-frontend/16-UI-SPEC.md

<interfaces>
Current `frontend/src/app/Router.jsx` structure:
```jsx
import { WeeklyPlanPage } from '../features/weekly-plan/index.js';  // TO ADD
// ... existing imports ...

function DashboardPlaceholder() {
  return (
    <div ...>
      <Link to="/profile">Profile, BMI & TDEE</Link>
      <Link to="/food-log">Log Food</Link>
      <Link to="/activities">Activity Recommendations</Link>
      {/* Add: <Link to="/weekly-plan">Weekly Plan</Link> */}
    </div>
  );
}

export default function Router() {
  return (
    <Routes>
      {/* ... existing routes ... */}
      {/* Add: <Route path="/weekly-plan" element={...} /> */}
    </Routes>
  );
}
```

The `WeeklyPlanPage` component is exported from `frontend/src/features/weekly-plan/index.js` as a named export:
```javascript
export { default as WeeklyPlanPage } from './components/WeeklyPlanPage.jsx';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register /weekly-plan route and add dashboard navigation link</name>
  <files>
    frontend/src/app/Router.jsx
  </files>
  <read_first>
    frontend/src/app/Router.jsx (current state — exact file to modify)
    frontend/src/features/weekly-plan/index.js (barrel export — confirm WeeklyPlanPage export name)
  </read_first>
  <action>
    Make two targeted edits to `frontend/src/app/Router.jsx`:

    **1. Add import for WeeklyPlanPage:**
    Add this import line after the existing ActivitiesPage import (around line 8):
    ```javascript
    import { WeeklyPlanPage } from '../features/weekly-plan/index.js';
    ```

    **2. Add route element:**
    Add this route AFTER the `/activities` route (around line 86) and BEFORE the `/` catch-all route:
    ```jsx
    <Route path="/weekly-plan" element={<ResponsiveLayout><ProtectedRoute><WeeklyPlanPage /></ProtectedRoute></ResponsiveLayout>} />
    ```

    **3. Add dashboard navigation link:**
    In the `DashboardPlaceholder` component, add a Link to `/weekly-plan` after the Activity Recommendations link (around line 70):
    ```jsx
    <Link to="/weekly-plan" style={{ padding: '0.75rem 1rem', border: '1px solid #ccc', borderRadius: '4px', textDecoration: 'none', color: '#333', minHeight: '44px', display: 'inline-flex', alignItems: 'center' }}>
      {'Weekly Plan'}
    </Link>
    ```

    The resulting Router.jsx should have the new route between the /activities route and the / (catch-all) route, and the nav link should appear after "Activity Recommendations" in the dashboard.

    **Do NOT modify or remove any existing code.** Only add the three additions described above. All imports, routes, and JSX must remain unchanged except for these three additions.
  </action>
  <acceptance_criteria>
    - `Router.jsx` contains `import { WeeklyPlanPage } from '../features/weekly-plan/index.js';`
    - `Router.jsx` contains `<Route path="/weekly-plan"` with `element={<ResponsiveLayout><ProtectedRoute><WeeklyPlanPage /></ProtectedRoute></ResponsiveLayout>}`
    - `DashboardPlaceholder` contains `<Link to="/weekly-plan"` with text "Weekly Plan"
    - All existing imports and routes remain unchanged
    - `/weekly-plan` route is inside ProtectedRoute (not publicly accessible)
  </acceptance_criteria>
  <verify>
    <automated>node -e "
      const f = require('fs').readFileSync('frontend/src/app/Router.jsx', 'utf-8');
      const checks = [
        f.includes(\"WeeklyPlanPage'\") || f.includes('WeeklyPlanPage'),
        f.includes('path=\"/weekly-plan\"') || f.includes('path=\"/weekly-plan\"'),
        f.includes('to=\"/weekly-plan\"') || f.includes('to=\"/weekly-plan\"'),
        f.includes('Weekly Plan'),
        f.includes('import { WeeklyPlanPage }'),
      ];
      const pass = checks.every(Boolean);
      checks.forEach((c, i) => console.log('check_' + i + ':', c));
      console.log('all_pass:', pass);
      process.exit(pass ? 0 : 1);
    "</automated>
  </verify>
  <done>/weekly-plan route registered in Router.jsx with ProtectedRoute wrapper and dashboard nav link added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→route | URL navigation crosses into app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Information Disclosure | /weekly-plan route | mitigate | Route wrapped in ProtectedRoute — unauthenticated users redirected to /login |
</threat_model>

<verification>
1. Router.jsx imports WeeklyPlanPage without errors
2. Route is registered before the / catch-all route
3. DashboardPlaceholder shows the new link
4. All existing routes and navigation links remain functional
</verification>

<success_criteria>
- Navigating to `/weekly-plan` renders the WeeklyPlanPage component
- Unauthenticated users are redirected to `/login`
- Dashboard shows "Weekly Plan" nav link
- All existing routes continue to work
</success_criteria>

<output>
After completion, create `.planning/phases/16-weekly-plan-frontend/16-03-SUMMARY.md`
</output>
