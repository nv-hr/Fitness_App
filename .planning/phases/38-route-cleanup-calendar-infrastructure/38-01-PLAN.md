---
phase: 38-route-cleanup-calendar-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/Router.jsx
  - frontend/src/shared/calendar/CalendarPageLayout.jsx
  - .planning/PROJECT.md
autonomous: true
requirements:
  - UI-ROUTE-01
  - UI-ROUTE-02
  - UI-ROUTE-03
user_setup: []

must_haves:
  truths:
    - "Navigating to /meal-calendar transparently redirects to /food-log with no flash"
    - "DashboardPlaceholder shows 3 nav links (Profile/Log Food/Activities), not 4"
    - "CalendarPageLayout accepts optional defaultDay prop and syncs selectedDay on prop changes"
    - "PROJECT.md references /activities instead of /activity-calendar"
  artifacts:
    - path: "frontend/src/app/Router.jsx"
      provides: "Route cleanup — /meal-calendar redirect, nav link removal"
      min_lines: 88
    - path: "frontend/src/shared/calendar/CalendarPageLayout.jsx"
      provides: "defaultDay prop with useEffect sync"
      exports: ["default"]
    - path: ".planning/PROJECT.md"
      provides: "Corrected route references"
      contains: "/activities"
  key_links:
    - from: "Router.jsx"
      to: "/food-log"
      via: "Navigate component"
      pattern: "Navigate.*to=\"/food-log\""
    - from: "Router.jsx DashboardPlaceholder"
      to: "3 nav links"
      pattern: "Link to=\"/(profile|food-log|activities)\""
    - from: "CalendarPageLayout"
      to: "selectedDay setter"
      via: "useEffect on defaultDay"
      pattern: "useEffect.*defaultDay"

---

<objective>
Clean up the `/meal-calendar` route with a client-side redirect, update DashboardPlaceholder nav links, enhance CalendarPageLayout with an optional `defaultDay` prop, and fix PROJECT.md route references.

**Purpose:** Unblock Phase 39/40 page merges by removing the stale `/meal-calendar` route, updating navigation, and adding the `defaultDay` CalendarPageLayout API that FoodLogPage (Phase 39) will use for date-aware behavior.

**Output:**
- Router.jsx: `/meal-calendar` replaced with `<Navigate to="/food-log" replace />`; "Meal Calendar" nav link removed; `MealCalendarPage` import removed
- CalendarPageLayout.jsx: `defaultDay` prop (default `null`) with `useEffect` that re-syncs `selectedDay` on every prop change
- PROJECT.md: 3 references to `/activity-calendar` corrected to `/activities`
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<![CDATA[
From frontend/src/shared/calendar/CalendarPageLayout.jsx (current signature):
export default function CalendarPageLayout({
  dayStatusMap,    // Map<string, string>
  loading,         // boolean
  error,           // Error | null
  onMonthChange,   // function(month) | undefined — external month change callback
  onDaySelect,     // function(day) | undefined — external day select callback
  children,        // ReactNode
})

State: const [currentMonth, setCurrentMonth] = useState(() => startOfMonth(new Date()));
State: const [selectedDay, setSelectedDay] = useState(null);
Selected day cleared to null during month navigation (in handleMonthChange/handleTodayClick).
External onDaySelect(null) called on month nav, external onDaySelect(day) called on day select.

From frontend/src/app/Router.jsx:
- line 7: import { FoodLogPage, MealCalendarPage } from '../features/food-log/index.js';
- lines 69-71: <Link to="/meal-calendar">Meal Calendar</Link> (nav link to remove)
- line 89: <Route path="/meal-calendar" element={...}> (route to replace with Navigate)
- Navigate import already exists at line 2: import { ..., Navigate, ... } from 'react-router-dom';

From .planning/PROJECT.md (references needing correction):
- line 18: "/activity-calendar" → "/activities" (feature description)
- line 48: "/activity-calendar" → "/activities" (route removal description — preserve semantic meaning)
- line 137: "/activity-calendar" → "/activities" (UI-03 requirement)
]]>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route cleanup — redirect /meal-calendar to /food-log, update nav</name>
  <files>
    frontend/src/app/Router.jsx
  </files>
  <action>
    Make three precise changes to Router.jsx:

    **Change 1 — Remove MealCalendarPage import (line 7):**
    ```
    // BEFORE:
    import { FoodLogPage, MealCalendarPage } from '../features/food-log/index.js';
    // AFTER:
    import { FoodLogPage } from '../features/food-log/index.js';
    ```

    **Change 2 — Remove "Meal Calendar" nav link from DashboardPlaceholder (lines 69-71):**
    Remove the entire `<Link to="/meal-calendar">` block (4 lines including indentation):
    ```
    <Link to="/meal-calendar" style={{ padding: '0.75rem 1rem', border: '1px solid #ccc', borderRadius: '4px', textDecoration: 'none', color: '#333', minHeight: '44px', display: 'inline-flex', alignItems: 'center' }}>
      {'Meal Calendar'}
    </Link>
    ```
    Per UI-SPEC §1: remaining 3 links (Profile/Log Food/Activities) are UNCHANGED in style, order, or text.
    Per D-02: Nav link label change: only removal, no "Meal Calendar" → "Food Log" rename (Food Log link already exists).

    **Change 3 — Replace /meal-calendar Route with Navigate redirect (line 89):**
    ```
    // BEFORE:
    <Route path="/meal-calendar" element={<ResponsiveLayout><ProtectedRoute><MealCalendarPage /></ProtectedRoute></ResponsiveLayout>} />
    // AFTER:
    <Route path="/meal-calendar" element={<Navigate to="/food-log" replace />} />
    ```
    Per D-01: client-side redirect using `<Navigate to="/food-log" replace />`. The `Navigate` component is already imported at line 2 from `react-router-dom`. No additional imports needed.

    **Verification:** The Navigate route must NOT be wrapped in ResponsiveLayout + ProtectedRoute — the redirect target's route already handles that. A bare `<Navigate>` is correct per D-01.
  </action>
  <verify>
    <automated>node -e "
      const fs = require('fs');
      const c = fs.readFileSync('frontend/src/app/Router.jsx', 'utf8');
      // Verify MealCalendarPage import removed
      const noMealImport = !c.includes('MealCalendarPage');
      // Verify Navigate route exists
      const hasNavigateRoute = c.includes('Navigate to=\"/food-log\"');
      // Verify Meal Calendar nav link removed
      const noMealNavLink = !c.includes('Meal Calendar');
      // Verify remaining nav links
      const profileLink = c.includes('to=\"/profile\"');
      const foodLogLink = c.includes('to=\"/food-log\"');
      const activitiesLink = c.includes('to=\"/activities\"');
      process.exit(noMealImport && hasNavigateRoute && noMealNavLink && profileLink && foodLogLink && activitiesLink ? 0 : 1);
    "</automated>
  </verify>
  <done>
    DashboardPlaceholder shows 3 nav links (Profile • Log Food • Activity Recommendations).
    Navigating to /meal-calendar immediately redirects to /food-log with no flash/loading state.
    MealCalendarPage import removed from Router.jsx (but continues to work via its own route path for backward compat during transition).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add optional defaultDay prop to CalendarPageLayout</name>
  <files>
    frontend/src/shared/calendar/CalendarPageLayout.jsx
  </files>
  <action>
    Enhance CalendarPageLayout with a `defaultDay` prop that auto-selects a day and re-syncs on every prop change.

    **Step 1 — Add useEffect to imports:**
    Change line 1 from:
    ```
    import { useState, useCallback } from 'react';
    ```
    to:
    ```
    import { useState, useEffect, useCallback } from 'react';
    ```

    **Step 2 — Add defaultDay to destructured props (line 22-28 area):**
    ```
    export default function CalendarPageLayout({
      dayStatusMap,
      loading,
      error,
      defaultDay = null,       // NEW: optional auto-select day
      onMonthChange: externalOnMonthChange,
      onDaySelect: externalOnDaySelect,
      children,
    }) {
    ```

    **Step 3 — Add useEffect after all useState/callback declarations but before the return statement:**
    Insert this between the `handleDaySelect` callback and the return statement (around line 56):
    ```jsx
      // Sync selectedDay whenever defaultDay prop changes (D-03: always-sync behavior)
      useEffect(() => {
        setSelectedDay(defaultDay);
      }, [defaultDay]);
    ```

    **Key contract per D-03/D-04:**
    - `defaultDay` defaults to `null` (backward-compatible — current behavior preserved)
    - When `defaultDay` is set to a non-null Date, `selectedDay` state updates immediately
    - The effect runs on EVERY prop change to `defaultDay` (not just mount), ensuring re-sync
    - `null` value sets selectedDay to null (matches current default behavior)
    - No visual change when `defaultDay` is null (default)
  </action>
  <verify>
    <automated>node -e "
      const fs = require('fs');
      const c = fs.readFileSync('frontend/src/shared/calendar/CalendarPageLayout.jsx', 'utf8');
      const hasUseEffect = c.includes('useEffect');
      const hasDefaultDay = c.includes('defaultDay = null') || c.includes('defaultDay =  null');
      const hasSyncEffect = c.includes('setSelectedDay(defaultDay)') || c.includes('setSelectedDay(defaultDay)');
      const hasUseEffectImport = c.includes('useEffect');
      process.exit(hasDefaultDay && hasSyncEffect ? 0 : 1);
    "</automated>
  </verify>
  <done>
    CalendarPageLayout accepts `defaultDay={someDate}` to auto-select a day.
    Passing `defaultDay={null}` (or omitting the prop) preserves existing behavior.
    Prop changes re-sync selectedDay via useEffect (always-sync, per D-03).
    Visual impact: none when defaultDay is null. When set, CalendarGrid highlights the selected day.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix PROJECT.md route references (/activity-calendar → /activities)</name>
  <files>
    .planning/PROJECT.md
  </files>
  <action>
    Per D-04 and UI-ROUTE-03: Correct three references to `/activity-calendar` in PROJECT.md that should refer to `/activities` (the actual route defined in Router.jsx).

    **Line 18:** Change `/activity-calendar` to `/activities` in:
    ```
    - **Activity Calendar Page** — Month grid view at `/activities` with color-coded days...
    ```

    **Line 48:** Change the `/activity-calendar` portion to `/activities` in:
    ```
    - Standalone `/activities` and `/meal-calendar` routes removed
    ```
    Rationale: The activity calendar page route has always been `/activities`, not `/activity-calendar`. The PROJECT.md had an incorrect route name.

    **Line 137:** Change the `/activity-calendar` portion to `/activities` in:
    ```
    - [ ] **UI-03**: Standalone `/activities` and `/meal-calendar` routes removed
    ```

    Use two edits: one for line 18 (the first occurrence, which is part of a description sentence), and one for lines 48 and 137 (which are bullet-list items using the same phrasing). A `replaceAll` can handle the two identical bullet items if they share exact text, but verify uniqueness first.

    **Only these three occurrences should change.** The ROADMAP.md `/activity-calendar` references in Phase 35's historical success criteria are correct — they describe the v1.7 state where `Router.jsx` also used `/activity-calendar` as a comment (the route name has since been `/activities`). Those are not in scope per D-04.
  </action>
  <verify>
    <automated>node -e "
      const fs = require('fs');
      const c = fs.readFileSync('.planning/PROJECT.md', 'utf8');
      // Should NOT contain /activity-calendar anymore
      const noOldRef = !c.includes('/activity-calendar');
      process.exit(noOldRef ? 0 : 1);
    "</automated>
  </verify>
  <done>
    PROJECT.md has zero references to `/activity-calendar`. All three occurrences corrected to `/activities`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→Router.jsx | URL path input from browser address bar |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | S (Spoofing) | `/meal-calendar` redirect | accept | Redirect target is same origin; no auth-sensitive data exposed via path |
| T-38-02 | T (Tampering) | CalendarPageLayout defaultDay | accept | optional UI-only prop — no data mutation involved |
</threat_model>

<verification>
- Router.jsx test: `node -e "..."` grep checks pass (3 nav links, no MealCalendarPage import, Navigate route exists)
- CalendarPageLayout test: `node -e "..."` grep checks pass (defaultDay prop, useEffect sync)
- PROJECT.md test: zero `/activity-calendar` occurrences remain
</verification>

<success_criteria>
- DashboardPlaceholder shows exactly 3 nav links (Profile, Log Food, Activity Recommendations)
- `/meal-calendar` URL redirects to `/food-log` without any loading flash
- CalendarPageLayout accepts `defaultDay` prop with `null` default (backward compatible)
- PROJECT.md corrected to reference `/activities` instead of `/activity-calendar`
- All original test suites pass (shared calendar tests unaffected)
</success_criteria>

<output>
After completion, create `.planning/phases/38-route-cleanup-calendar-infrastructure/38-01-SUMMARY.md`
</output>
