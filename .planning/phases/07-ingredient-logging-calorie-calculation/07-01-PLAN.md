---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/food-log/components/FoodLogPage.jsx
  - frontend/src/features/food-log/api/foodLogApi.js
autonomous: true
requirements: [LOG-07, LOG-08]
must_haves:
  truths:
    - "User sees calorie preview update in real-time as they type weight"
    - "Preview shows format: '{portionGrams}g = {calories} kkal'"
    - "Preview hides when weight field is empty or invalid"
    - "Logged food entry sends foodId + portionGrams to server for calorie calculation"
  artifacts:
    - path: "frontend/src/features/food-log/components/FoodLogPage.jsx"
      provides: "Live calorie preview between portion input and meal type selector"
      contains: "calculateCalories preview logic"
    - path: "frontend/src/features/food-log/api/foodLogApi.js"
      provides: "API client functions (no changes needed — logFood already accepts foodId + portionGrams)"
  key_links:
    - from: "FoodLogPage.jsx portion input onChange"
      to: "calorie preview display"
      via: "calculateCalories() frontend call"
      pattern: "Math\\.round\\(.*caloriesPer100g.*portion.*\\/.*100"
    - from: "FoodLogPage.jsx handleLogFood"
      to: "/api/food/log"
      via: "logFood API call with foodId + portionGrams"
      pattern: "logFood\\(\\{"
---

<objective>
Add live calorie preview to the food logging flow and ensure ingredient selection + weight entry works end-to-end.

Purpose: Users see calculated calories before confirming a log entry (LOG-08), and can select ingredients and enter weight in grams (LOG-07).
Output: Modified FoodLogPage.jsx with real-time calorie preview, verified server-side calculation flow.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ingredient-logging-calorie-calculation/07-CONTEXT.md
@.planning/phases/07-ingredient-logging-calorie-calculation/07-RESEARCH.md

@frontend/src/features/food-log/components/FoodLogPage.jsx
@frontend/src/features/food-log/api/foodLogApi.js
@backend/src/services/food.service.js
@backend/src/controllers/food.controller.js
@frontend/src/shared/i18n/translations.js

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/src/services/food.service.js:
```javascript
export function calculateCalories(caloriesPer100g, portionGrams) {
  return Math.round((caloriesPer100g * portionGrams) / 100);
}
```

From frontend/src/features/food-log/api/foodLogApi.js:
```javascript
export async function logFood(data) {
  // data: { foodId, portionGrams, logDate, mealType } for seeded foods
  // data: { customFoodName, calories, portionGrams, logDate, mealType } for custom one-off
  return apiPost('/api/food/log', data);
}
```

From FoodLogPage.jsx selectedFood state shape:
```javascript
// When food selected from search:
{ id: number, name: string, calories_per_100g: number, category: string }
// When custom one-off:
{ name: string, calories_per_100g: number }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add frontend calorie preview calculation and display</name>
  <files>frontend/src/features/food-log/components/FoodLogPage.jsx</files>
  <read_first>
    - frontend/src/features/food-log/components/FoodLogPage.jsx (current state — selectedFood panel at line 162-214)
    - backend/src/services/food.service.js (calculateCalories formula — line 59-61)
  </read_first>
  <behavior>
    - When selectedFood exists AND portion is a valid number (1-5000): display "{portion}g = {calories} kkal" preview
    - When portion is empty, non-numeric, or out of range (0 or >5000): hide preview entirely (no placeholder)
    - Preview uses identical formula to server: Math.round((caloriesPer100g * portion) / 100)
    - Preview appears inline below portion input, above meal type selector (per D-02)
    - Preview rounds to whole number (per D-04)
  </behavior>
  <action>
In FoodLogPage.jsx, add a derived `previewCalories` variable computed from selectedFood.calories_per_100g and the current portion value:

1. Add a helper function inside the component (before the return statement):
```javascript
const previewCalories = (() => {
  const grams = parseInt(portion, 10);
  if (!selectedFood?.calories_per_100g || isNaN(grams) || grams < 1 || grams > 5000) {
    return null;
  }
  return Math.round((selectedFood.calories_per_100g * grams) / 100);
})();
```

2. In the selectedFood panel (line 162-214), insert the preview display between the portion input `</div>` (after line 189) and the meal type selector `<div>` (line 192). The preview should render as:
```jsx
{previewCalories !== null && (
  <p style={{ margin: '0.5rem 0', fontSize: '0.875rem', color: '#666' }}>
    {parseInt(portion, 10)}g = {previewCalories} kkal
  </p>
)}
```

3. Do NOT change handleFoodSelect (keep default portion at '100' — line 67).
4. Do NOT change handleLogFood (server already calculates calories correctly — lines 79-119).

Per D-01: Preview updates in real-time as user types weight (achieved via React state re-render on portion onChange).
Per D-02: Preview appears inline below weight input, same section as food name and kkal/100g.
Per D-03: Preview hidden when weight empty/invalid (previewCalories returns null).
Per D-04: Uses Math.round — identical to server-side calculateCalories().
  </action>
  <verify>
    <automated>node -e "const m=Math.round((165*150)/100); console.log(m===248?'PASS':'FAIL: '+m)"</automated>
  </verify>
  <acceptance_criteria>
    - FoodLogPage.jsx contains `previewCalories` derived variable with null-return guard for invalid portion
    - FoodLogPage.jsx contains JSX conditional `{previewCalories !== null &&` rendering preview text
    - Preview text format matches: `{parseInt(portion, 10)}g = {previewCalories} kkal`
    - Preview uses `Math.round` (not Math.floor or Math.ceil)
    - Preview positioned between portion input closing `</div>` and meal type selector opening `<div>`
    - No changes to handleFoodSelect or handleLogFood functions
  </acceptance_criteria>
  <done>Live calorie preview displays "{portion}g = {calories} kkal" when valid weight entered, hides when empty/invalid, uses identical formula to server</done>
</task>

<task type="auto">
  <name>Task 2: Verify end-to-end ingredient selection + weight logging flow</name>
  <files>frontend/src/features/food-log/components/FoodLogPage.jsx</files>
  <read_first>
    - frontend/src/features/food-log/components/FoodLogPage.jsx (full file after Task 1 changes)
    - backend/src/controllers/food.controller.js (logFood endpoint — line 50-101)
    - frontend/src/features/food-log/api/foodLogApi.js (logFood function — line 11-13)
  </read_first>
  <action>
Verify the existing flow works correctly for ingredient-based logging:

1. Confirm handleFoodSelect (line 65-70) sets selectedFood with {id, name, calories_per_100g} from search results — this is the ingredient selection path (LOG-07).

2. Confirm handleLogFood (line 79-119) sends {foodId, portionGrams, logDate, mealType} for seeded foods — the server calculates calories from calories_per_100g (LOG-08).

3. Confirm the server-side logFood endpoint (food.controller.js line 68-74) fetches food by ID and calls calculateCalories(food.calories_per_100g, portionGrams).

4. No code changes needed — this task verifies the existing flow satisfies LOG-07 and LOG-08. The only change from Task 1 is the visual preview; the logging mechanism is already correct.

5. If any issue found (e.g., selectedFood.id not being passed correctly), fix it.
  </action>
  <verify>
    <automated>grep -n "foodId: selectedFood.id" frontend/src/features/food-log/components/FoodLogPage.jsx</automated>
  </verify>
  <acceptance_criteria>
    - FoodLogPage.jsx handleLogFood contains `foodId: selectedFood.id` in logFood call
    - food.controller.js logFood contains `calculateCalories(food.calories_per_100g, portionGrams)`
    - food.service.js calculateCalories uses formula `(caloriesPer100g * portionGrams) / 100`
    - grep confirms: `foodId: selectedFood.id` exists in FoodLogPage.jsx
    - grep confirms: `calculateCalories(food.calories_per_100g, portionGrams)` exists in food.controller.js
  </acceptance_criteria>
  <done>Ingredient selection + weight entry + server-side calorie calculation flow verified end-to-end (LOG-07, LOG-08)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API (POST /api/food/log) | Untrusted portionGrams value — must validate range server-side |
| client→preview display | Client-side calorie calculation is display-only; server recalculates (source of truth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | food.controller.js logFood | mitigate | Server validates portionGrams range (1-5000) before calculation — already implemented (line 55-57) |
| T-07-02 | Spoofing | Client calorie preview | accept | Preview is display-only; server recalculates from canonical calories_per_100g. No security impact if preview shows wrong value. |
</threat_model>

<verification>
- grep confirms `previewCalories` variable exists in FoodLogPage.jsx with null-return guard
- grep confirms `Math.round` used in preview calculation (matches server formula)
- grep confirms `foodId: selectedFood.id` in logFood call
- grep confirms server-side `calculateCalories(food.calories_per_100g, portionGrams)` in food.controller.js
- Preview text format: `{portion}g = {calories} kkal` renders conditionally
</verification>

<success_criteria>
- User selects ingredient from search, enters weight, sees live calorie preview before logging
- Preview updates in real-time as weight changes
- Preview hides when weight is empty or invalid
- Logged entry stores correct server-calculated calories
- LOG-07 and LOG-08 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-ingredient-logging-calorie-calculation/07-01-SUMMARY.md`
</output>
