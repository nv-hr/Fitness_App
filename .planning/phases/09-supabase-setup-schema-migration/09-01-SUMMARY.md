---
phase: 09-supabase-setup-schema-migration
plan: 01
subsystem: database
tags: [postgres, supabase, ddl, seed-data, identity-columns, jsonb]

# Dependency graph
requires:
  - phase: 08-english-ui-migration
    provides: MySQL schema with English ENUM values (meal_type)
provides:
  - PostgreSQL schema DDL with 5 ENUMs, 6 tables, indexes, FK constraints
  - Re-runnable seed data with 35 activities and 201 food ingredients
affects: [10-backend-query-rewrite, 11-docker-restructure]

# Tech tracking
tech-stack:
  added: [PostgreSQL DDL patterns: GENERATED BY DEFAULT AS IDENTITY, TIMESTAMPTZ, JSONB, IF NOT EXISTS]
  patterns: [Idempotent CREATE TYPE IF NOT EXISTS / CREATE TABLE IF NOT EXISTS, re-runnable seed with TRUNCATE+ON CONFLICT, ::jsonb cast syntax]

key-files:
  created: [backend/db/schema.sql, backend/db/seed.sql]
  modified: []

key-decisions:
  - "D-08: PostgreSQL ENUMs for gender, fitness_goal, activity_level, food_category, meal_type"
  - "D-09: GENERATED BY DEFAULT AS IDENTITY (not SERIAL)"
  - "D-10: TIMESTAMPTZ for timestamps, DEFAULT NOW() for created_at"
  - "D-11: Application-managed updated_at — TIMESTAMPTZ DEFAULT NOW(), no PG trigger"
  - "D-12: JSONB for goal_tags and equipment_needed with DEFAULT '[]'::jsonb"
  - "D-13: Manual SQL translation for seed data (not pgloader)"
  - "D-14: JSON arrays use '[\"value\"]'::jsonb syntax"
  - "D-16: Separate files — schema.sql (DDL only) and seed.sql (data only)"

patterns-established:
  - "Idempotent DDL: all CREATE TYPE and CREATE TABLE use IF NOT EXISTS"
  - "Re-runnable seed: TRUNCATE ... CASCADE before INSERTs, ON CONFLICT DO NOTHING on each block"
  - "PostgreSQL-specific syntax: ::jsonb casts, GENERATED BY DEFAULT AS IDENTITY"

requirements-completed: [SUP-02, SUP-03]

# Metrics
duration: 3 min
completed: 2026-05-27
---

# Phase 9 Plan 1: PostgreSQL Schema & Seed Translation Summary

**PostgreSQL schema DDL with 6 tables, 5 ENUMs, indexes, and 201-ingredient seed data for Supabase migration**

## Performance

- **Duration:** 3 min
- **Started:** 2026-05-27T14:19:33Z
- **Completed:** 2026-05-27T14:22:53Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Translated 6 MySQL tables to PostgreSQL with GENERATED BY DEFAULT AS IDENTITY, TIMESTAMPTZ, JSONB, and BOOLEAN types
- Created 5 PostgreSQL ENUMs (gender, fitness_goal, activity_level, food_category, meal_type) with idempotent CREATE TYPE IF NOT EXISTS
- Added indexes (idx_name, idx_user_category, idx_user_date, idx_user_recent) with IF NOT EXISTS
- All foreign keys preserved: ON DELETE CASCADE for users/profiles/foods/food_logs/user_activity_log, ON DELETE SET NULL for food_logs.food_id
- 35 activities with ::jsonb casts for goal_tags and equipment_needed
- 201 food ingredients across 8 categories, re-runnable via TRUNCATE + ON CONFLICT DO NOTHING

## Task Commits

Each task was committed atomically:

1. **Task 1: Write PostgreSQL schema DDL (schema.sql)** - `b358750` (feat)
2. **Task 2: Write PostgreSQL seed data (seed.sql)** - `fb80eba` (feat)

**Plan metadata:** *(pending final commit)*

## Files Created

- `backend/db/schema.sql` (121 lines) — PostgreSQL DDL: 5 ENUMs, 6 tables, indexes, FK constraints. Idempotent via IF NOT EXISTS.
- `backend/db/seed.sql` (302 lines) — PostgreSQL seed data: 35 activities, 201 foods. Re-runnable via TRUNCATE + ON CONFLICT DO NOTHING.

## Decisions Made

All locked decisions from 09-CONTEXT.md were applied as specified:
- D-08 through D-12: PostgreSQL-specific type mappings (ENUMs, IDENTITY, TIMESTAMPTZ, JSONB)
- D-13/D-14: Manual translation with ::jsonb syntax
- D-16: Separate DDL and seed files

No new decisions — plan executed exactly as specified.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## Verification Results

### Automated Checks

**schema.sql:**
- 5 CREATE TYPE IF NOT EXISTS statements — PASS
- 6 CREATE TABLE IF NOT EXISTS statements — PASS
- No SERIAL, TINYINT, ENGINE=, AUTO_INCREMENT, ON UPDATE CURRENT_TIMESTAMP — PASS
- No TIMESTAMP without TZ — PASS
- No JSON without B — PASS
- File: 121 lines — PASS

**seed.sql:**
- TRUNCATE activities CASCADE — PASS
- TRUNCATE foods CASCADE — PASS
- ALTER SEQUENCE ... RESTART for both sequences — PASS
- 35 activity rows (1 INSERT block) — PASS
- 201 food rows (8 INSERT blocks) — PASS
- ::jsonb patterns: 70 occurrences (35 activities × 2 columns) — PASS
- ON CONFLICT DO NOTHING on every INSERT block (9 total) — PASS
- user_id=NULL on all food rows — PASS
- is_custom=FALSE on all food rows — PASS
- File: 302 lines — PASS

## Next Phase Readiness

- DDL and seed files ready for Phase 10 (Backend Query Rewrite) and Phase 11 (Docker Restructure)
- Plan 09-02 (Supabase Project Setup) and Plan 09-03 (Connection Verification) remain in this phase
- No blockers for downstream work

---

*Phase: 09-supabase-setup-schema-migration*
*Completed: 2026-05-27*
