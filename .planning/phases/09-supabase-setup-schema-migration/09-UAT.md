---
status: testing
phase: 09-supabase-setup-schema-migration
source:
  - 09-01-SUMMARY.md
  - 09-02-SUMMARY.md
started: "2026-05-27T14:30:00.000Z"
updated: "2026-05-27T14:30:00.000Z"
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: |
  All generated files exist and pass syntax checks:
  - schema.sql: 5 ENUMs, 6 tables, PostgreSQL-native types, IF NOT EXISTS
  - seed.sql: 35 activities, 201+ foods, ::jsonb casts, TRUNCATE + ON CONFLICT
  - verify-supabase-connection.js: pg Pool, SSL, dotenv, exit codes
  - supabase/config.toml with project_id
  - pg in package.json
  - DATABASE_URL in .env
result: pass
note: "verify script needed module resolution fix (scripts/ dir outside backend/node_modules). Fixed and confirmed: 'Supabase connected successfully', { connected: 1 }, exit 0."

### 2. PostgreSQL Schema Correctness
expected: |
  backend/db/schema.sql contains:
  - 5 CREATE TYPE IF NOT EXISTS statements (gender, fitness_goal, activity_level, food_category, meal_type)
  - 6 CREATE TABLE IF NOT EXISTS statements (users, profiles, foods, food_logs, activities, user_activity_log)
  - GENERATED BY DEFAULT AS IDENTITY for PKs (no SERIAL)
  - TIMESTAMPTZ for all timestamps (no TIMESTAMP without TZ)
  - JSONB for goal_tags/equipment_needed (no JSON without B)
  - BOOLEAN for pdp_consent/is_custom (no TINYINT)
  - No MySQL-specific syntax (ENGINE=, AUTO_INCREMENT, ON UPDATE CURRENT_TIMESTAMP)
result: pass

### 3. Seed Data Correctness
expected: |
  backend/db/seed.sql contains:
  - TRUNCATE activities CASCADE and TRUNCATE foods CASCADE at top (re-runnable)
  - ALTER SEQUENCE ... RESTART for both sequences
  - Exactly 35 activity INSERTs with ::jsonb casts for goal_tags and equipment_needed
  - 201+ food ingredient INSERTs across 8 category blocks
  - user_id=NULL and is_custom=FALSE on all food rows
  - ON CONFLICT DO NOTHING on every INSERT block
result: pass

### 4. Supabase CLI Project Initialized
expected: |
  supabase/config.toml exists with a [project] section containing project_id.
  supabase/.gitignore exists with standard exclusions (.branches, .temp, .env).
  No supabase login or link has been run (those need user interaction).
result: pass

### 5. pg Driver Installation
expected: |
  backend/package.json has "pg" in dependencies.
  Running `npm ls pg` from backend/ shows pg and its dependencies resolved.
  No changes to backend/src/server.js or backend/src/config/database.js.
result: pass

### 6. Connection Verify Script
expected: |
  scripts/verify-supabase-connection.js exists and contains:
  - Pool creation with SSL config (rejectUnauthorized: false)
  - SELECT 1 AS connected query
  - process.exit(0) on success, process.exit(1) on failure
  - dotenv config pointing to ../backend/.env
result: pass

### 7. DATABASE_URL Configuration
expected: |
  backend/.env contains DATABASE_URL line with placeholder format:
  postgresql://postgres:YOUR_PASSWORD@db.YOUR_REF.supabase.co:6543/postgres
  No real credentials or secrets committed.
  Root .gitignore already excludes .env.
result: pass

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
