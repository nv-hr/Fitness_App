---
phase: 04-frontend-static-analysis-deduplication
plan: "03"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/src/shared/ui/index.js
autonomous: false
requirements:
  - FE-01
  - FE-02
  - FE-03

must_haves:
  truths:
    - "fallow dead-code reports zero unused-export issues for shared/ui barrel (post-deprecation + post-extraction)"
    - "All frontend tests pass with no regressions"
    - "fallow dupes reports significantly fewer clone groups than the Plan 01 baseline"
    - "Phase 4 is fully complete: dead code deprecated, duplication extracted, tests green"
  artifacts:
    - path: ".planning/phases/04-frontend-static-analysis-deduplication/04-03-SUMMARY.md"
      provides: "Final verification report with before/after fallow metrics"
  key_links:
    - from: "04-01-fallow-dead-code.json"
      to: "post-refactor fallow run"
      via: "total_issues comparison"
      pattern: "total_issues"
---

<objective>
Verify the complete Phase 4 changes by re-running fallow analysis post-refactoring, confirming all tests pass, and producing the final phase SUMMARY.md. This plan also addresses any additional fallow-flagged issues discovered during Plans 01-02 that require a second-pass cleanup (per FE-01, FE-02, FE-03).

Purpose: Close the verification loop — confirm the dead code baseline from Plan 01 is reduced, the duplication from Plan 02 is gone, and no regressions were introduced. This is the phase gate before marking Phase 4 complete.

Output: Clean fallow report; green tests; documented before/after metrics.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-static-analysis-deduplication/04-CONTEXT.md
@.planning/phases/04-frontend-static-analysis-deduplication/04-01-SUMMARY.md
@.planning/phases/04-frontend-static-analysis-deduplication/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Address any remaining fallow findings (second-pass cleanup)</name>
  <files>
    frontend/src/shared/ui/index.js
  </files>
  <action>
    Re-run fallow dead-code with the same flags as Plan 01:

    ```
    npx fallow dead-code --format json --quiet 2>/dev/null || true
    ```

    Compare `total_issues` in the new output against the Plan 01 baseline stored in `04-01-SUMMARY.md`.

    For any NEWLY RESOLVED issues (count dropped): note them as fixed.

    For any REMAINING issues that are NOT false positives and NOT already moved to `_deprecated/`:
    - Apply the same D-02 strategy: `git mv` the file to a `_deprecated/` subfolder
    - Update the barrel `index.js` if a shared/ui export is involved

    SKIP items that are false positives (lucide-react workspace dep, items with dynamic imports not detectable by static analysis). Document the reason in the SUMMARY.

    If the `useCountdownTimer.js` hook or `countdown.js` lib are confirmed unused by fallow (no imports found), move them to `shared/hooks/_deprecated/` and `shared/lib/_deprecated/` respectively using `git mv`.

    IMPORTANT: Do NOT modify production logic. Only move confirmed-dead files. If uncertain, leave in place and document as "needs manual review."
  </action>
  <verify>
    <automated>
      # Run fallow and capture post-refactor count
      $result = npx fallow dead-code --format json --quiet 2>/dev/null | ConvertFrom-Json
      Write-Host "Post-refactor total_issues: $($result.total_issues)"
    </automated>
  </verify>
  <done>fallow dead-code total_issues count is lower than the Plan 01 baseline, or all remaining issues are documented as intentional/false-positive.</done>
</task>

<task type="auto">
  <name>Task 2: Run full frontend test suite and produce final SUMMARY</name>
  <files>
    .planning/phases/04-frontend-static-analysis-deduplication/04-03-SUMMARY.md
  </files>
  <action>
    Run the full frontend test suite:

    ```powershell
    # From project root
    npm test --workspace=frontend
    # OR if not configured in scripts:
    cd frontend && npx vitest run --reporter=verbose
    ```

    All tests must pass. If a test fails:
    - Read the test file to understand what it expects
    - Check if the refactoring in Plan 02 broke an import path or component structure
    - Fix the import in the test file (test file path updates are acceptable) or in the component (if logic was accidentally changed)
    - Do NOT remove or skip tests to make them pass

    ---

    Run final fallow dupes comparison:
    ```
    npx fallow dupes --format json --quiet --mode mild 2>/dev/null || true
    ```

    ---

    Create `.planning/phases/04-frontend-static-analysis-deduplication/04-03-SUMMARY.md` with:

    ## Phase 4 — Final Verification Report

    ### Before/After Metrics

    | Metric | Before (Plan 01 baseline) | After (Post-refactor) | Delta |
    |--------|--------------------------|----------------------|-------|
    | fallow dead-code total_issues | {from 04-01-SUMMARY} | {new count} | -{N} |
    | fallow dupes clone groups | {from 04-01-SUMMARY} | {new count} | -{N} |
    | Deprecated files created | 0 | {count} | +{N} |
    | Shared components extracted | 0 | 2 (MetricItem, Card) | +2 |
    | Tests passing | {baseline} | {count} | ✓ |

    ### Files Changed

    List all files created, modified, or moved across Plans 01-03.

    ### Decisions Honored

    - D-01 (fallow analysis): ✓ Used fallow for all dead code identification
    - D-02 (deprecated strategy): ✓ Files moved to `_deprecated/` — not deleted
    - D-03 (component extraction): ✓ MetricItem (semantic) and Card (primitive) extracted to shared/ui/

    ### Remaining Open Items

    List any items that need follow-up (false positives documented, files left as "needs manual review", etc.)
  </action>
  <verify>
    <automated>
      Test-Path ".planning/phases/04-frontend-static-analysis-deduplication/04-03-SUMMARY.md"
    </automated>
  </verify>
  <done>
    - All frontend tests pass (0 failures)
    - fallow dead-code total_issues is lower than Plan 01 baseline
    - 04-03-SUMMARY.md contains before/after metrics table
    - All three CONTEXT.md decisions (D-01, D-02, D-03) are marked as honored
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4 complete: fallow analysis run, dead code moved to _deprecated/, MetricItem and Card components extracted, all tests passing. Final SUMMARY.md created with before/after metrics.
  </what-built>
  <how-to-verify>
    1. Start the frontend dev server: `npm run dev --workspace=frontend` (or `cd frontend && npm run dev`)
    2. Navigate to the Activities page — confirm the activity summary card renders correctly (metrics still show with icons, labels, values, and units)
    3. Navigate to the Food Log page — confirm the calorie summary card renders correctly (progress bar still works, deficit/surplus status still shows)
    4. Check that the PageHeader component is no longer available in the app (it was unused, so nothing should break)
    5. Optionally: open browser DevTools → Console — confirm no import errors or missing module warnings
  </how-to-verify>
  <resume-signal>Type "approved" if the UI looks correct and no console errors. Or describe any visual differences you see.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fallow CLI output | Read-only JSON; no code execution |
| Test suite | Runs existing test files; read-only verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Repudiation | Phase completion claim | mitigate | SUMMARY.md with before/after fallow metrics provides audit trail |
| T-04-06 | Denial of Service | Accidentally deleting a live component | mitigate | D-02 strategy: only `git mv` to _deprecated/, git history always recoverable |
</threat_model>

<verification>
Final phase gate checklist:

```powershell
# 1. All tests pass
npm test --workspace=frontend

# 2. fallow dead-code issues reduced
npx fallow dead-code --format json --quiet 2>/dev/null | ConvertFrom-Json | Select-Object total_issues

# 3. MetricItem used in both target components
Select-String -Path "frontend/src/features/activities/components/ActivitySummary.jsx" -Pattern "MetricItem"
Select-String -Path "frontend/src/features/food-log/components/CalorieSummary.jsx" -Pattern "MetricItem"

# 4. PageHeader not exported from barrel
$pageHeaderExport = Select-String -Path "frontend/src/shared/ui/index.js" -Pattern "PageHeader"
if ($pageHeaderExport) { "FAIL: PageHeader still exported" } else { "PASS: PageHeader removed from barrel" }
```

All four checks must pass before marking Phase 4 complete.
</verification>

<success_criteria>
- `npm test --workspace=frontend` exits with 0 failures
- fallow dead-code total_issues is measurably lower than the Plan 01 baseline
- MetricItem and Card are used in ActivitySummary and CalorieSummary
- PageHeader is in `_deprecated/` and NOT in the barrel
- 04-03-SUMMARY.md exists with before/after metrics table
- Human visual verification approved (checkpoint passed)
- All three CONTEXT.md decisions honored and documented
</success_criteria>

<output>
Create `.planning/phases/04-frontend-static-analysis-deduplication/04-03-SUMMARY.md` when done.
</output>
