---
phase: 03-tdee-calculator-goals
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/features/profile/components/ProfileForm.jsx
  - frontend/src/features/profile/components/TdeeResult.jsx
  - frontend/src/features/profile/api/profileApi.js
  - frontend/src/shared/i18n/translations.js
autonomous: true
requirements: [TDEE-01, TDEE-02, TDEE-03]
must_haves:
  truths:
    - "User can select activity level (low/medium/high) on profile form"
    - "TDEE result displays as a range with activity level description"
    - "Calorie target adjusted for fitness goal is displayed"
    - "Indonesian disclaimer text appears below TDEE result"
  artifacts:
    - path: "frontend/src/features/profile/components/TdeeResult.jsx"
      provides: "TDEE range display with disclaimer"
      min_lines: 25
    - path: "frontend/src/features/profile/components/ProfileForm.jsx"
      provides: "Activity level selector + TdeeResult display"
      contains: "activityLevel"
    - path: "frontend/src/features/profile/api/profileApi.js"
      provides: "activityLevel in create/update payloads"
      exports: ["createProfile", "getProfile", "updateProfile"]
    - path: "frontend/src/shared/i18n/translations.js"
      provides: "TDEE/activity level Indonesian translations"
      contains: "tdee."
  key_links:
    - from: "frontend/src/features/profile/components/ProfileForm.jsx"
      to: "frontend/src/features/profile/components/TdeeResult.jsx"
      via: "import and conditional render"
      pattern: "import.*TdeeResult"
    - from: "frontend/src/features/profile/api/profileApi.js"
      to: "backend/src/controllers/profile.controller.js"
      via: "PUT/POST /api/profile with activityLevel"
      pattern: "activityLevel"
---

<objective>
Add TDEE UI to the profile page: activity level selector, TdeeResult component displaying range and calorie target, Indonesian translations, and API adapter updates.

Purpose: Deliver the user-facing TDEE experience on the existing /profile page, fulfilling TDEE-01, TDEE-02, and TDEE-03 from the frontend.
Output: TdeeResult component, updated ProfileForm with activity selector, updated API adapter, new translations.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-tdee-calculator-goals/03-CONTEXT.md
@.planning/phases/03-tdee-calculator-goals/03-RESEARCH.md
@.planning/phases/02-profile-bmi-calculator/02-02-SUMMARY.md

@frontend/src/features/profile/components/ProfileForm.jsx
@frontend/src/features/profile/components/BmiResult.jsx
@frontend/src/features/profile/api/profileApi.js
@frontend/src/shared/i18n/translations.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TdeeResult component and add Indonesian translations</name>
  <files>frontend/src/features/profile/components/TdeeResult.jsx, frontend/src/shared/i18n/translations.js</files>
  <read_first>
    - frontend/src/features/profile/components/BmiResult.jsx (pattern to follow for TdeeResult)
    - frontend/src/shared/i18n/translations.js (existing translation keys)
  </read_first>
  <action>
    1. Create frontend/src/features/profile/components/TdeeResult.jsx following the BmiResult.jsx pattern:
       - Props: { tdee, tdeeRange, calorieTarget, activityLevel, fitnessGoal }
       - Display structure:
         - Label: t('tdee.yourTdee')
         - TDEE range: "{tdeeRange.min} - {tdeeRange.max} {t('tdee.caloriesPerDay')}"
         - Activity level description: t('tdee.activity.' + activityLevel) — e.g., "Olahraga 3-5x per minggu"
         - Calorie target section:
           - Label: t('tdee.calorieTarget')
           - Value: "{calorieTarget} {t('tdee.caloriesPerDay')}"
           - Goal label: t('profile.' + fitnessGoal) — reuses existing profile translations
         - Disclaimer: t('tdee.disclaimer') — "Hasil ini adalah estimasi dan bukan diagnosis medis."
       - Styling: Same container style as BmiResult (marginTop: '1.5rem', padding: '1rem', border: '1px solid #E5E7EB', borderRadius: '8px')
       - Use a green color (#22C55E) for the calorie target badge (same as BMI "normal" category)

    2. Add translations to frontend/src/shared/i18n/translations.js — add a `tdee` section:
       ```
       tdee: {
         yourTdee: 'Kebutuhan Kalori Harian Anda',
         caloriesPerDay: 'kkal/hari',
         calorieTarget: 'Target Kalori Harian',
         disclaimer: 'Hasil ini adalah estimasi dan bukan diagnosis medis.',
         activity: {
           low: 'Jarang berolahraga, pekerjaan duduk',
           medium: 'Olahraga 3-5x per minggu',
           high: 'Olahraga intensif setiap hari',
         },
       }
       ```
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const comp=fs.readFileSync('frontend/src/features/profile/components/TdeeResult.jsx','utf8'); console.log(comp.includes('tdeeRange') && comp.includes('calorieTarget') ? 'PASS: TdeeResult has required props' : 'FAIL'); const trans=fs.readFileSync('frontend/src/shared/i18n/translations.js','utf8'); console.log(trans.includes('yourTdee') && trans.includes('activity') ? 'PASS: translations added' : 'FAIL')"</automated>
  </verify>
  <done>TdeeResult.jsx created with range display, calorie target, activity description, and Indonesian disclaimer; translations.js has tdee section with all required keys</done>
</task>

<task type="auto">
  <name>Task 2: Update ProfileForm with activity level selector and TdeeResult display</name>
  <files>frontend/src/features/profile/components/ProfileForm.jsx</files>
  <read_first>
    - frontend/src/features/profile/components/ProfileForm.jsx (current form structure)
    - frontend/src/features/profile/components/TdeeResult.jsx (created in Task 1)
  </read_first>
  <action>
    1. Add activityLevel to the Zod validation schema:
       - Add: `activityLevel: z.enum(['low', 'medium', 'high']).optional(),`
       - This field is optional since existing profiles may not have it set yet

    2. Add activityLevel to defaultValues:
       - Add: `activityLevel: 'medium',` (reasonable default)

    3. Add activity level selector to the form — place it after the fitnessGoal select and before the submit button:
       - Use a <select> element with id="activityLevel"
       - Three options:
         - value="low": t('profile.activityLow') — "Rendah (Jarang olahraga)"
         - value="medium": t('profile.activityMedium') — "Sedang (Olahraga 3-5x/minggu)"
         - value="high": t('profile.activityHigh') — "Tinggi (Olahraga setiap hari)"
       - Same styling as existing selects (display: 'block', width: '100%', padding: '0.5rem', etc.)
       - Label: t('profile.activityLevel') — "Tingkat Aktivitas"

    4. Add state for TDEE result:
       - Add: `const [tdeeResult, setTdeeResult] = useState(null);`

    5. Update the loadProfile useEffect to extract TDEE data from response:
       - After setting bmiResult, also set: `setTdeeResult({ tdee: response.data.tdee, tdeeRange: response.data.tdeeRange, calorieTarget: response.data.calorieTarget, activityLevel: profile.activity_level, fitnessGoal: profile.fitness_goal });`
       - Only set if response.data.tdee is not null

    6. Update the onSubmit handler to extract TDEE data:
       - After setting bmiResult, also set: `setTdeeResult({ tdee: response.data.tdee, tdeeRange: response.data.tdeeRange, calorieTarget: response.data.calorieTarget, activityLevel: data.activityLevel, fitnessGoal: data.fitnessGoal });`
       - Only set if response.data.tdee is not null

    7. Update the form submission payload to include activityLevel:
       - In both createProfile(data) and updateProfile(data) calls, ensure data includes activityLevel
       - The react-hook-form will include it automatically since it's registered

    8. Add TdeeResult display below BmiResult:
       - After the BmiResult conditional render, add:
         `{tdeeResult && tdeeResult.tdee && (<TdeeResult tdee={tdeeResult.tdee} tdeeRange={tdeeResult.tdeeRange} calorieTarget={tdeeResult.calorieTarget} activityLevel={tdeeResult.activityLevel} fitnessGoal={tdeeResult.fitnessGoal} />)}`

    9. Also update the profile title translation key to reflect TDEE:
       - Change t('profile.title') from 'Profil & Kalkulator BMI' to 'Profil, BMI & TDEE' in translations.js
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const form=fs.readFileSync('frontend/src/features/profile/components/ProfileForm.jsx','utf8'); const checks = ['activityLevel', 'TdeeResult', 'tdeeResult']; checks.forEach(c => console.log(form.includes(c) ? 'PASS: ' + c + ' found' : 'FAIL: ' + c + ' missing'))"</automated>
  </verify>
  <done>ProfileForm has activity level selector with 3 options, TDEE result state management, TdeeResult conditional display below BMI, and form submission includes activityLevel</done>
</task>

<task type="auto">
  <name>Task 3: Update API adapter and add remaining translations</name>
  <files>frontend/src/features/profile/api/profileApi.js, frontend/src/shared/i18n/translations.js</files>
  <read_first>
    - frontend/src/features/profile/api/profileApi.js (current API functions)
    - frontend/src/shared/i18n/translations.js (current translations)
  </read_first>
  <action>
    1. Update frontend/src/features/profile/api/profileApi.js:
       - In createProfile(): Add `activityLevel: data.activityLevel,` to the request body
       - In updateProfile(): Add `activityLevel: data.activityLevel,` to the request body
       - getProfile() needs no change (GET request, no body)

    2. Add remaining translations to frontend/src/shared/i18n/translations.js in the profile section:
       - `activityLevel: 'Tingkat Aktivitas',`
       - `activityLow: 'Rendah (Jarang olahraga)',`
       - `activityMedium: 'Sedang (Olahraga 3-5x/minggu)',`
       - `activityHigh: 'Tinggi (Olahraga setiap hari)',`
       - Update `title: 'Profil, BMI & TDEE',` (was 'Profil & Kalkulator BMI')
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const api=fs.readFileSync('frontend/src/features/profile/api/profileApi.js','utf8'); console.log((api.match(/activityLevel/g)||[]).length >= 2 ? 'PASS: activityLevel in API adapter' : 'FAIL'); const trans=fs.readFileSync('frontend/src/shared/i18n/translations.js','utf8'); console.log(trans.includes('activityLevel') && trans.includes('activityLow') ? 'PASS: activity translations added' : 'FAIL')"</automated>
  </verify>
  <done>API adapter sends activityLevel in create/update requests; translations include activity level labels and updated profile title</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | Activity level selection sent as untrusted input |
| API→browser | TDEE calculation results returned to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | ProfileForm.jsx | mitigate | Zod validation restricts activityLevel to enum ['low', 'medium', 'high'] — invalid values rejected client-side |
| T-03-05 | Repudiation | profileApi.js | accept | No audit logging required for TDEE calculations in v1; calculations are deterministic and reproducible |
</threat_model>

<verification>
- All 3 tasks complete with automated verification passing
- TdeeResult.jsx renders with tdee, tdeeRange, calorieTarget, activityLevel, fitnessGoal props
- ProfileForm has activity level <select> with 3 options (low/medium/high)
- API adapter includes activityLevel in createProfile and updateProfile request bodies
- All UI text uses t() translation keys — no hardcoded Indonesian strings
- Profile title updated to 'Profil, BMI & TDEE'
- TDEE section only renders when tdee value is non-null
</verification>

<success_criteria>
- TdeeResult component created following BmiResult pattern with range display, calorie target, activity description, and disclaimer
- ProfileForm has activity level selector with Indonesian labels (Rendah/Sedang/Tinggi)
- Form submission includes activityLevel in request payload
- API adapter sends activityLevel to backend on create/update
- TDEE result displays below BMI result when activity level is set
- All UI text in Bahasa Indonesia via t() function
- Disclaimer "Hasil ini adalah estimasi dan bukan diagnosis medis." appears below TDEE result
</success_criteria>

<output>
After completion, create `.planning/phases/03-tdee-calculator-goals/03-02-SUMMARY.md`
</output>
