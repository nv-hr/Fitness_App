---
phase: 06-international-ingredient-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/db/init.sql
autonomous: true
requirements:
  - INGR-01
  - INGR-02
  - INGR-03
  - INGR-04
must_haves:
  truths:
    - "Database contains 200+ international ingredients with accurate calories_per_100g values"
    - "Ingredients are organized into 8 English categories (proteins, carbs, vegetables, fruits, dairy, fats, drinks, other)"
    - "Old Indonesian food rows are removed, replaced with international ingredients"
    - "Existing food_logs retain their calories values; food_id becomes NULL (SET NULL on delete)"
  artifacts:
    - path: "backend/db/init.sql"
      provides: "Updated foods table ENUM + 200+ international ingredient seed data"
      contains: "ENUM('proteins', 'carbs', 'vegetables', 'fruits', 'dairy', 'fats', 'drinks', 'other')"
  key_links:
    - from: "backend/db/init.sql"
      to: "backend/src/services/food.service.js"
      via: "VALID_CATEGORIES must match ENUM values"
      pattern: "ENUM\\('proteins'.*\\)"
---

<objective>
Update the foods table schema and replace Indonesian food seed data with 200+ international ingredients.

Purpose: This is the database foundation for the international ingredient database. The ENUM change and seed data replacement must happen before backend or frontend updates.
Output: Updated init.sql with new ENUM and comprehensive international ingredient seeds.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-international-ingredient-database/06-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-food-database-calorie-logging/04-01-SUMMARY.md

@backend/db/init.sql — Current schema with Indonesian ENUM and 105 Indonesian food seeds
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update foods table ENUM and add migration comment</name>
  <files>backend/db/init.sql</files>
  <read_first>
    - backend/db/init.sql (lines 39-50 — current foods table schema)
  </read_first>
  <action>
In backend/db/init.sql, modify the `foods` table `category` column ENUM from Indonesian values to English values.

Current ENUM (line 44):
```sql
category ENUM('makanan_pokok', 'lauk', 'sayur', 'buah', 'minuman', 'snack', 'lainnya') NOT NULL,
```

Replace with:
```sql
category ENUM('proteins', 'carbs', 'vegetables', 'fruits', 'dairy', 'fats', 'drinks', 'other') NOT NULL,
```

Add a migration comment block above the foods table CREATE statement explaining the truncate+reseed strategy:
```sql
-- ============================================================
-- Phase 06 Migration: Indonesian foods → International ingredients
-- Strategy: TRUNCATE foods table, reseed with international ingredients
-- Existing food_logs: food_id becomes NULL (ON DELETE SET NULL FK)
-- Existing food_logs: calories values preserved (stored as INT, not FK-dependent)
-- ============================================================
```

Do NOT add a TRUNCATE statement to init.sql — init.sql uses CREATE TABLE IF NOT EXISTS, so it won't re-run on existing databases. The truncate+reseed will be handled at deployment time by the developer running:
```sql
TRUNCATE TABLE foods;
-- Then re-run init.sql or the seed INSERT statements
```

Do NOT create a separate migration file — this phase modifies init.sql directly (single-file schema pattern established in Phase 04).
  </action>
  <verify>
    <automated>grep -c "ENUM('proteins', 'carbs', 'vegetables', 'fruits', 'dairy', 'fats', 'drinks', 'other')" backend/db/init.sql</automated>
  </verify>
  <acceptance_criteria>
    - init.sql contains `ENUM('proteins', 'carbs', 'vegetables', 'fruits', 'dairy', 'fats', 'drinks', 'other')` in the foods table definition
    - init.sql does NOT contain any of the old Indonesian category values in the foods table ENUM: 'makanan_pokok', 'lauk', 'sayur', 'buah', 'minuman', 'snack', 'lainnya'
    - Migration comment block present above foods table CREATE statement mentioning Phase 06 and truncate+reseed strategy
  </acceptance_criteria>
  <done>ENUM updated to 8 English categories, migration comment added, no old Indonesian values remain in foods table definition</done>
</task>

<task type="auto">
  <name>Task 2: Replace Indonesian food seeds with 200+ international ingredients</name>
  <files>backend/db/init.sql</files>
  <read_first>
    - backend/db/init.sql (lines 152-281 — current Indonesian food seed data)
    - .planning/phases/06-international-ingredient-database/06-CONTEXT.md (ingredient data decisions)
  </read_first>
  <action>
In backend/db/init.sql, replace ALL existing Indonesian food INSERT statements (lines 155-281) with international ingredient seed data organized by the 8 new categories.

Requirements for seed data:
- **Minimum 200 ingredients total** (exceeds INGR-01 requirement of "comprehensive coverage")
- **Each ingredient has calories_per_100g** (INGR-02) — use accurate USDA-based values
- **Ingredient names use descriptors** — "chicken breast" not "chicken", "brown rice" not "rice", "whole egg" not "egg"
- **All seeded foods**: `user_id=NULL, is_custom=FALSE`
- **Organized by category** with section header comments (same pattern as Phase 04)

Category distribution targets (aim for balanced coverage):

| Category | Target Count | Example Ingredients |
|----------|-------------|---------------------|
| proteins | 35+ | chicken breast, salmon fillet, ground beef, tofu firm, black beans, tuna canned, pork tenderloin, lamb chop, shrimp raw, egg whole |
| carbs | 30+ | white rice cooked, brown rice cooked, whole wheat bread, oats rolled, quinoa cooked, sweet potato baked, pasta spaghetti cooked, potato boiled, corn kernels, barley cooked |
| vegetables | 30+ | broccoli raw, spinach raw, carrot raw, bell pepper red, tomato raw, cucumber raw, onion raw, zucchini raw, cauliflower raw, kale raw, lettuce iceberg, mushroom white |
| fruits | 25+ | apple raw with skin, banana raw, orange raw, strawberry raw, blueberry raw, mango raw, grape raw, watermelon raw, pineapple raw, peach raw |
| dairy | 20+ | whole milk, skim milk, cheddar cheese, greek yogurt plain, mozzarella cheese, butter salted, cream cheese, cottage cheese, parmesan cheese, heavy cream |
| fats | 15+ | olive oil, coconut oil, almond butter, peanut butter, avocado oil, sunflower seeds, walnuts, chia seeds, flaxseed, sesame oil |
| drinks | 15+ | orange juice, apple juice, whole milk (also in dairy), black coffee, green tea, coca-cola, almond milk, soy milk, coconut water, sports drink |
| other | 15+ | honey, sugar white granulated, salt table, black pepper, soy sauce, ketchup, mayonnaise, mustard, vinegar white, chocolate dark 70% |

Use INSERT statements grouped by category (one INSERT per category with multiple values, same pattern as Phase 04):
```sql
-- proteins (35 items)
INSERT INTO foods (user_id, name, calories_per_100g, category, is_custom) VALUES
(NULL, 'Chicken breast, raw, skinless', 165, 'proteins', FALSE),
(NULL, 'Salmon fillet, raw', 208, 'proteins', FALSE),
...
;
```

Calorie values should be realistic USDA-based estimates per 100g. Round to nearest integer.

Keep the existing activities and user_activity_log seed data (lines 70-147) — do NOT modify or remove them.

Keep the food_logs table definition unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('backend/db/init.sql','utf8'); const inserts=c.match(/INSERT INTO foods/g)||[]; const total=c.split('INSERT INTO foods').slice(1).join('').match(/\(/g)||[]; console.log('INSERT blocks:', inserts.length, 'Total rows approx:', total.length); const cats=['proteins','carbs','vegetables','fruits','dairy','fats','drinks','other']; cats.forEach(cat=>{const re=new RegExp(\"'\"+cat+\"'\",'g'); const count=(c.match(re)||[]).length; console.log(cat+':', count);}); const oldCats=['makanan_pokok','lauk','sayur','buah','minuman','snack','lainnya']; const hasOld=oldCats.some(c=>c.includes(c)); console.log('Has old categories in seeds:', hasOld);"</automated>
  </verify>
  <acceptance_criteria>
    - init.sql contains INSERT INTO foods statements for all 8 new categories: 'proteins', 'carbs', 'vegetables', 'fruits', 'dairy', 'fats', 'drinks', 'other'
    - Total ingredient rows >= 200 (count values in INSERT statements)
    - Each category has at least 15 ingredients
    - No Indonesian food names remain in seed data (no 'Nasi', 'Rendang', 'Tempe', 'Gado-gado', etc.)
    - All seed rows use `user_id=NULL, is_custom=FALSE`
    - Section header comments present for each category (e.g., `-- proteins (35 items)`)
    - Activities seed data (lines 70-147) remains unchanged
  </acceptance_criteria>
  <done>200+ international ingredients seeded across 8 categories with accurate calories_per_100g, no Indonesian foods remain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init.sql → MySQL | Seed data is static SQL — no user input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | init.sql seed data | accept | Seed data is static, version-controlled SQL. No runtime user input. Low-value target. |
| T-06-02 | Information Disclosure | foods table ENUM change | accept | ENUM values are schema metadata, not sensitive data. No PII involved. |
| T-06-03 | Denial of Service | TRUNCATE + reseed at deployment | mitigate | Developer runs TRUNCATE manually during deployment window. Application should handle empty foods table gracefully (search returns 0 results, not 500). Existing food_logs unaffected (SET NULL FK). |
</threat_model>

<verification>
- ENUM in init.sql matches all 8 English categories exactly
- 200+ ingredient rows present across all 8 categories
- No Indonesian food names or category values remain in seed data
- Activities and user_activity_log seed data unchanged
- Migration comment block present explaining truncate+reseed strategy
</verification>

<success_criteria>
- init.sql can be run on a fresh MySQL instance without errors
- foods table has correct ENUM with 8 English categories
- 200+ international ingredients are seeded with accurate calories_per_100g
- All 8 categories have at least 15 ingredients each
- No Indonesian food data remains in the seed section
</success_criteria>

<output>
After completion, create `.planning/phases/06-international-ingredient-database/06-01-SUMMARY.md`
</output>
