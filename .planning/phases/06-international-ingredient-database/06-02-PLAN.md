---
phase: 06-international-ingredient-database
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/food.service.js
  - backend/src/controllers/food.controller.js
autonomous: true
requirements:
  - INGR-01
  - INGR-02
  - INGR-03
must_haves:
  truths:
    - "Backend validates custom food categories against the new English category set"
    - "Validation error messages remain in Indonesian (UI language not changed until Phase 8)"
    - "Existing search, logging, and summary APIs continue to work without modification"
  artifacts:
    - path: "backend/src/services/food.service.js"
      provides: "Updated VALID_CATEGORIES array matching new ENUM"
      contains: "VALID_CATEGORIES = ['proteins', 'carbs', 'vegetables', 'fruits', 'dairy', 'fats', 'drinks', 'other']"
  key_links:
    - from: "backend/src/services/food.service.js"
      to: "backend/db/init.sql"
      via: "VALID_CATEGORIES must match foods table ENUM"
      pattern: "VALID_CATEGORIES.*proteins"
---

<objective>
Update backend service validation to use new English ingredient categories.

Purpose: The VALID_CATEGORIES constant in food.service.js must match the new ENUM values in init.sql. Without this, custom food creation will reject valid English categories. The existing repository, controller, and routes require no changes — they work with category-agnostic queries.
Output: Updated food.service.js with English VALID_CATEGORIES.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-international-ingredient-database/06-CONTEXT.md

@backend/src/services/food.service.js — Current VALID_CATEGORIES with Indonesian values
@backend/src/controllers/food.controller.js — Uses validateFoodData from food.service.js
@.planning/phases/04-food-database-calorie-logging/04-02-SUMMARY.md — Backend API patterns from Phase 04
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update VALID_CATEGORIES in food.service.js</name>
  <files>backend/src/services/food.service.js</files>
  <read_first>
    - backend/src/services/food.service.js (line 3 — current VALID_CATEGORIES)
  </read_first>
  <action>
In backend/src/services/food.service.js, update the VALID_CATEGORIES constant on line 3.

Current value:
```js
const VALID_CATEGORIES = ['makanan_pokok', 'lauk', 'sayur', 'buah', 'minuman', 'snack', 'lainnya'];
```

Replace with:
```js
const VALID_CATEGORIES = ['proteins', 'carbs', 'vegetables', 'fruits', 'dairy', 'fats', 'drinks', 'other'];
```

Do NOT change the validation error messages — they remain in Indonesian because the UI language migration happens in Phase 8. The validateFoodData function should still throw:
- `'Nama makanan wajib diisi (1-100 karakter)'` for invalid name
- `'Kalori per 100g harus antara 0-5000 kkal'` for invalid calories
- `'Kategori makanan tidak valid'` for invalid category (this message stays Indonesian even though the valid categories are now English)

Do NOT modify any other functions in food.service.js:
- `getSeededFoodCount` — works as-is
- `getFoodsByCategory` — works as-is (iterates VALID_CATEGORIES, which is now updated)
- `validateFoodData` — works as-is (uses updated VALID_CATEGORIES)
- `calculateCalories` — works as-is (pure function, no category dependency)
  </action>
  <verify>
    <automated>grep "VALID_CATEGORIES = \['proteins', 'carbs', 'vegetables', 'fruits', 'dairy', 'fats', 'drinks', 'other'\]" backend/src/services/food.service.js</automated>
  </verify>
  <acceptance_criteria>
    - food.service.js line 3 contains `const VALID_CATEGORIES = ['proteins', 'carbs', 'vegetables', 'fruits', 'dairy', 'fats', 'drinks', 'other']`
    - food.service.js does NOT contain any Indonesian category values: 'makanan_pokok', 'lauk', 'sayur', 'buah', 'minuman', 'snack', 'lainnya'
    - Validation error messages remain in Indonesian (grep for 'Nama makanan wajib', 'Kalori per 100g harus', 'Kategori makanan tidak valid')
    - All 4 exported functions remain unchanged: getSeededFoodCount, getFoodsByCategory, validateFoodData, calculateCalories
  </acceptance_criteria>
  <done>VALID_CATEGORIES updated to 8 English categories, Indonesian error messages preserved, all functions unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Verify no other backend files reference Indonesian category values</name>
  <files>backend/src/controllers/food.controller.js</files>
  <read_first>
    - backend/src/controllers/food.controller.js (full file)
    - backend/src/repositories/food.repository.js (full file)
  </read_first>
  <action>
Grep all backend source files for Indonesian category values to confirm no other references exist:

```bash
grep -r "makanan_pokok\|'lauk'\|'sayur'\|'buah'\|'minuman'\|'snack'\|'lainnya'" backend/src/ --include="*.js"
```

Expected result: NO matches in any .js file. The only remaining references to Indonesian categories should be in:
- `backend/db/init.sql` — in the old Indonesian food INSERT statements (which Plan 01 removes)
- `frontend/src/features/food-log/components/FoodSearch.jsx` — in categoryLabels (updated by Plan 03)

If any .js file in backend/src/ contains Indonesian category values, remove or update them to use the new English values.

Specifically check food.controller.js — it has `VALID_MEAL_TYPES` for meal types (sarapan, makan_siang, makan_malam, camilan). Do NOT change these — meal type ENUM migration happens in Phase 8, not this phase. Only verify that no food category values (makanan_pokok, lauk, etc.) appear.
  </action>
  <verify>
    <automated>grep -r "makanan_pokok\|'lauk'\|'sayur'\|'buah'\|'minuman'\|'snack'\|'lainnya'" backend/src/ --include="*.js" | grep -v "VALID_MEAL_TYPES" | grep -v "sarapan\|makan_siang\|makan_malam\|camilan"</automated>
  </verify>
  <acceptance_criteria>
    - grep returns no matches for Indonesian food category values in backend/src/*.js files
    - VALID_MEAL_TYPES in food.controller.js remains unchanged (meal types are Phase 8)
    - No Indonesian category values exist in any backend JavaScript file
  </acceptance_criteria>
  <done>Confirmed zero Indonesian food category references in backend JavaScript files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/food | Custom food creation accepts category from untrusted client input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | validateFoodData in food.service.js | mitigate | Server-side validation rejects invalid categories via VALID_CATEGORIES whitelist. Parameterized queries in repository prevent SQL injection. |
| T-06-05 | Spoofing | Custom food category input | mitigate | VALID_CATEGORIES array acts as strict whitelist. Any category not in the array throws ValidationError (400). |
</threat_model>

<verification>
- VALID_CATEGORIES in food.service.js matches init.sql ENUM exactly
- No Indonesian category values remain in backend JavaScript files
- Validation error messages remain in Indonesian (Phase 8 migration)
- VALID_MEAL_TYPES unchanged (meal types are Phase 8)
</verification>

<success_criteria>
- food.service.js VALID_CATEGORIES contains exactly 8 English categories matching init.sql ENUM
- grep confirms zero Indonesian food category values in backend/src/*.js
- Custom food creation accepts only the 8 new English categories
- All existing API endpoints continue to function without modification
</success_criteria>

<output>
After completion, create `.planning/phases/06-international-ingredient-database/06-02-SUMMARY.md`
</output>
