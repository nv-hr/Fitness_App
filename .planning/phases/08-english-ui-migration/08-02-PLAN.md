---
phase: 08-english-ui-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/src/features/food-log/components/FoodSearch.jsx
  - frontend/src/features/food-log/components/FoodLogPage.jsx
  - frontend/src/features/food-log/components/FoodLogTable.jsx
  - frontend/src/features/food-log/components/CalorieSummary.jsx
  - frontend/src/features/food-log/components/CalorieHistory.jsx
  - frontend/src/features/food-log/components/CustomFoodForm.jsx
  - frontend/src/features/activities/components/ActivitiesPage.jsx
  - frontend/src/features/activities/components/ActivityCard.jsx
  - backend/src/controllers/food.controller.js
  - backend/src/services/food.service.js
  - backend/src/services/profile.service.js
autonomous: true
requirements:
  - UI-04

must_haves:
  truths:
    - "No hardcoded 'kkal' strings remain in any frontend component"
    - "No hardcoded Indonesian strings remain in any frontend component"
    - "All backend error messages are in English"
  artifacts:
    - path: "frontend/src/features/food-log/components/FoodSearch.jsx"
      provides: "English 'kcal/100g' display"
    - path: "frontend/src/features/food-log/components/FoodLogTable.jsx"
      provides: "English 'Total', 'kcal', 'last' labels"
    - path: "frontend/src/features/food-log/components/CalorieHistory.jsx"
      provides: "English 'Date', 'Calories', 'Entries' headers"
    - path: "frontend/src/features/food-log/components/CustomFoodForm.jsx"
      provides: "English validation messages, 'Cancel' button"
    - path: "backend/src/controllers/food.controller.js"
      provides: "English error messages"
    - path: "backend/src/services/food.service.js"
      provides: "English validation messages"
    - path: "backend/src/services/profile.service.js"
      provides: "English validation messages"
  key_links:
    - from: "frontend components"
      to: "UI display"
      via: "Hardcoded strings replaced with English"
      pattern: "kkal|Batal|Tanggal|Kalori|Entri|Total"
    - from: "backend controllers/services"
      to: "API error responses"
      via: "ValidationError and errorResponse messages"
      pattern: "new ValidationError\\(|errorResponse\\("
---

<objective>
Replace all hardcoded Indonesian strings and 'kkal' unit across frontend components and backend error messages. These strings bypass the translation system and must be manually updated.

Purpose: Ensure no Indonesian text leaks through hardcoded strings in JSX or backend error responses.
Output: All frontend components and backend services use English strings exclusively.
</objective>

<execution_context>
@C:/Users/LENOVO/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/LENOVO/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-english-ui-migration/08-CONTEXT.md
@frontend/src/features/food-log/components/FoodSearch.jsx
@frontend/src/features/food-log/components/FoodLogPage.jsx
@frontend/src/features/food-log/components/FoodLogTable.jsx
@frontend/src/features/food-log/components/CalorieSummary.jsx
@frontend/src/features/food-log/components/CalorieHistory.jsx
@frontend/src/features/food-log/components/CustomFoodForm.jsx
@frontend/src/features/activities/components/ActivitiesPage.jsx
@frontend/src/features/activities/components/ActivityCard.jsx
@backend/src/controllers/food.controller.js
@backend/src/services/food.service.js
@backend/src/services/profile.service.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded Indonesian strings and 'kkal' in frontend components</name>
  <files>
    frontend/src/features/food-log/components/FoodSearch.jsx
    frontend/src/features/food-log/components/FoodLogPage.jsx
    frontend/src/features/food-log/components/FoodLogTable.jsx
    frontend/src/features/food-log/components/CalorieSummary.jsx
    frontend/src/features/food-log/components/CalorieHistory.jsx
    frontend/src/features/food-log/components/CustomFoodForm.jsx
  </files>
  <read_first>
    - frontend/src/features/food-log/components/FoodSearch.jsx
    - frontend/src/features/food-log/components/FoodLogPage.jsx
    - frontend/src/features/food-log/components/FoodLogTable.jsx
    - frontend/src/features/food-log/components/CalorieSummary.jsx
    - frontend/src/features/food-log/components/CalorieHistory.jsx
    - frontend/src/features/food-log/components/CustomFoodForm.jsx
  </read_first>
  <action>
Replace hardcoded strings in each file. Apply these exact replacements:

**FoodSearch.jsx (line 90):**
- `{food.calories_per_100g} kkal/100g` → `{food.calories_per_100g} kcal/100g`

**FoodLogPage.jsx (lines 183, 203):**
- `{selectedFood.calories_per_100g} kkal/100g` → `{selectedFood.calories_per_100g} kcal/100g`
- `{previewCalories} kkal` → `{previewCalories} kcal`

**FoodLogTable.jsx:**
- Line 60: `{log.portion_grams}g — {log.calories} kkal` → `{log.portion_grams}g — {log.calories} kcal`
- Line 76: `<span>Total</span>` → `<span>{t('foodLog.total')}</span>` — BUT since 'total' key doesn't exist in translations, add it first: in translations.js add `total: 'Total'` to foodLog section, then use `t('foodLog.total')` here. Actually, 'Total' is already English — just keep it as `<span>Total</span>` (no change needed for this one).
- Line 77: `{totalCalories} kkal` → `{totalCalories} kcal`
- Line 102: `— terakhir: {food.last_portion_grams}g` → `— last: {food.last_portion_grams}g`
- Line 104: `({food.calories} kkal)` → `({food.calories} kcal)`

**CalorieSummary.jsx (lines 17, 18, 43, 44):**
- `{totalConsumed} kkal` → `{totalConsumed} kcal` (line 17)
- `{calorieTarget} kkal` → `{calorieTarget} kcal` (line 18)
- `+${totalConsumed - calorieTarget} kkal` → `+${totalConsumed - calorieTarget} kcal` (line 43)
- `${remaining} kkal` → `${remaining} kcal` (line 44)

**CalorieHistory.jsx (lines 32, 33, 34, 49):**
- `<span>Tanggal</span>` → `<span>{t('foodLog.date')}</span>` — add `date: 'Date'` to foodLog section in translations.js first
- `<span style={{ textAlign: 'right' }}>Kalori</span>` → `<span style={{ textAlign: 'right' }}>{t('foodLog.calories')}</span>` — add `calories: 'Calories'` to foodLog section in translations.js first
- `<span style={{ textAlign: 'right' }}>Entri</span>` → `<span style={{ textAlign: 'right' }}>{t('foodLog.entries')}</span>` — add `entries: 'Entries'` to foodLog section in translations.js first
- `{day.total_calories} kkal` → `{day.total_calories} kcal`

**CustomFoodForm.jsx:**
- Line 9: `'Maksimal 100 karakter'` → `'Maximum 100 characters'`
- Line 10: `'Minimal 0 kkal'` → `'Minimum 0 kcal'`
- Line 10: `'Maksimal 5000 kkal'` → `'Maximum 5000 kcal'`
- Line 84: `Batal` → `{t('auth.cancel')}` — add `cancel: 'Cancel'` to auth section in translations.js first

**IMPORTANT:** For CalorieHistory.jsx and CustomFoodForm.jsx changes that reference new translation keys, ALSO edit translations.js to add:
- `foodLog.date: 'Date'`
- `foodLog.calories: 'Calories'`
- `foodLog.entries: 'Entries'`
- `auth.cancel: 'Cancel'`

Do NOT change: component structure, styling, logic, imports, or any strings already in English.
  </action>
  <acceptance_criteria>
    - grep -r 'kkal' frontend/src returns 0 matches
    - grep -r 'Tanggal\|Kalori\|Entri\|terakhir\|Batal' frontend/src returns 0 matches
    - grep -r 'Maksimal\|Minimal' frontend/src returns 0 matches
    - CalorieHistory.jsx uses t() for 'Date', 'Calories', 'Entries' headers
    - CustomFoodForm.jsx uses t('auth.cancel') for Cancel button
    - All 'kkal' replaced with 'kcal' across all listed files
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['frontend/src/features/food-log/components/FoodSearch.jsx','frontend/src/features/food-log/components/FoodLogPage.jsx','frontend/src/features/food-log/components/FoodLogTable.jsx','frontend/src/features/food-log/components/CalorieSummary.jsx','frontend/src/features/food-log/components/CalorieHistory.jsx','frontend/src/features/food-log/components/CustomFoodForm.jsx']; let fail=false; files.forEach(f=>{const c=fs.readFileSync(f,'utf8'); if(c.includes('kkal')){console.log('FAIL: kkal found in',f);fail=true;} }); if(fail) process.exit(1); console.log('PASS: No kkal in frontend components');"</automated>
  </verify>
  <done>All hardcoded 'kkal' → 'kcal', all hardcoded Indonesian strings replaced with English or t() calls</done>
</task>

<task type="auto">
  <name>Task 2: Replace Indonesian error messages in backend controllers and services</name>
  <files>
    backend/src/controllers/food.controller.js
    backend/src/services/food.service.js
    backend/src/services/profile.service.js
  </files>
  <read_first>
    - backend/src/controllers/food.controller.js
    - backend/src/services/food.service.js
    - backend/src/services/profile.service.js
  </read_first>
  <action>
Replace all Indonesian error messages with English. Apply these exact replacements:

**food.controller.js:**
- Line 8: `const VALID_MEAL_TYPES = ['sarapan', 'makan_siang', 'makan_malam', 'camilan']` → `const VALID_MEAL_TYPES = ['breakfast', 'lunch', 'dinner', 'snack']` (per D-18)
- Line 17: `'Pencarian minimal 2 karakter'` → `'Search query must be at least 2 characters'`
- Line 56: `'Porsi harus antara 1-5000 gram'` → `'Portion must be between 1-5000 grams'`
- Line 61: `'Jenis makanan tidak valid'` → `'Invalid meal type'`
- Line 72: `'Makanan tidak ditemukan'` → `'Food not found'`
- Line 78: `'Kalori wajib diisi'` → `'Calories are required'`
- Line 82: `'foodId atau customFoodName wajib diisi'` → `'foodId or customFoodName is required'`

**food.service.js:**
- Line 40: `'Nama makanan wajib diisi (1-100 karakter)'` → `'Food name is required (1-100 characters)'`
- Line 44: `'Kalori per 100g harus antara 0-5000 kkal'` → `'Calories per 100g must be between 0-5000 kcal'`
- Line 48: `'Kategori makanan tidak valid'` → `'Invalid food category'`
- Line 66: `'Nama makanan wajib diisi (1-100 karakter)'` → `'Food name is required (1-100 characters)'`
- Line 70: `'Kalori per 100g harus antara 0-5000 kkal'` → `'Calories per 100g must be between 0-5000 kcal'`

**profile.service.js:**
- Line 100: `'Berat badan harus antara 2-300 kg'` → `'Weight must be between 2-300 kg'`
- Line 103: `'Tinggi badan harus antara 50-250 cm'` → `'Height must be between 50-250 cm'`
- Line 106: `'Umur harus antara 5-120 tahun'` → `'Age must be between 5-120 years'`
- Line 109: `'Jenis kelamin harus male, female, atau other'` → `'Gender must be male, female, or other'`
- Line 112: `'Tujuan fitness harus lose_weight, maintain, atau gain_weight'` → `'Fitness goal must be lose_weight, maintain, or gain_weight'`

Do NOT change: function signatures, logic, validation rules, or any English strings.
  </action>
  <acceptance_criteria>
    - food.controller.js VALID_MEAL_TYPES array contains ['breakfast', 'lunch', 'dinner', 'snack']
    - No Indonesian strings in food.controller.js error messages (grep for 'Pencarian', 'Porsi', 'Jenis', 'Makanan', 'Kalori', 'foodId')
    - No Indonesian strings in food.service.js (grep for 'Nama', 'Kalori', 'Kategori')
    - No Indonesian strings in profile.service.js (grep for 'Berat', 'Tinggi', 'Umur', 'Jenis', 'Tujuan')
    - 'kkal' replaced with 'kcal' in food.service.js validation messages
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['backend/src/controllers/food.controller.js','backend/src/services/food.service.js','backend/src/services/profile.service.js']; let fail=false; files.forEach(f=>{const c=fs.readFileSync(f,'utf8'); const id=['Pencarian','Porsi','Jenis makanan','Makanan tidak','Kalori wajib','Nama makanan','Kategori makanan','Berat badan','Tinggi badan','Umur harus','Jenis kelamin','Tujuan fitness']; const found=id.filter(w=>c.includes(w)); if(found.length){console.log('FAIL: Indonesian in',f,found);fail=true;} }); if(fail) process.exit(1); console.log('PASS: All backend errors in English');"</automated>
  </verify>
  <done>All backend error messages in English, VALID_MEAL_TYPES updated to English values per D-18</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→Backend API | Error messages are server-generated, not user-controlled — no injection risk |
| ValidationError messages | Messages returned to client in error responses — must not leak internal details |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Information Disclosure | Backend error messages | accept | Error messages are user-facing validation messages only — no internal stack traces, DB details, or system paths exposed. |
| T-08-03 | Integrity | VALID_MEAL_TYPES constant | mitigate | Ensure VALID_MEAL_TYPES in food.controller.js matches the database ENUM after migration (Plan 03). Mismatch causes all meal type validations to fail. |
</threat_model>

<verification>
- No 'kkal' in any frontend component file
- No Indonesian strings in any frontend component or backend service file
- VALID_MEAL_TYPES in food.controller.js matches ['breakfast', 'lunch', 'dinner', 'snack']
- Frontend builds without errors (npm run build in frontend/)
</verification>

<success_criteria>
- All hardcoded Indonesian strings replaced with English (per D-23, D-24)
- All 'kkal' → 'kcal' in display strings (per D-21)
- Backend error messages in clear English (per D-23)
- VALID_MEAL_TYPES constant updated to English values (per D-18)
- UI-04 satisfied: all UI text, error messages, navigation in English
</success_criteria>

<output>
After completion, create `.planning/phases/08-english-ui-migration/08-02-SUMMARY.md`
</output>
